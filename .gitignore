# Claude Code — local-only artifacts (rest of .claude/ is committed)
.claude/settings.local.json
.claude/worktrees/
.claude/agent-memory/

app/vendor/
app/var/
app/.env.local
app/.phpunit.cache/

# Symfony auto-generated config reference — IDE autocomplete only, regenerated on
# every dev cache warmup (drifts with the framework version); nothing at runtime or
# CI consumes it (PHPStan excludePaths + Rector withSkip already ignore it).
app/config/reference.php
*.cache
.idea/
.vscode/
*.swp
docker/mysql/data/
.env.local
node_modules/
test-results/
playwright-report/
playwright/.cache/
backups/

# HMAI-343: generated OpenAPI contract — a build artifact (`make openapi-dump`,
# CI openapi-contract job); regenerated from the annotated controllers, never committed.
openapi.json

# Local-only planning notes (not committed)
PLAN.md
TODO.md
