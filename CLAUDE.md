# AIHomeManager — Claude Code Context

Single-user system for automating everyday activities. Stack: PHP 8.5 + Symfony 8 + MySQL 8.4 LTS + Redis 8 + RabbitMQ 4.x. Hexagonal architecture, CQRS with two buses.

**Modules:** Series, Tasks, Books, Articles, Music, YouTubeProgress (+ **Goals** — module complete, **released in 1.19.0** (2026-07-11), epic HMAI-248 / fixVersion 1.19.0. **Domain skeleton (HMAI-249):** `src/Module/Goals/Domain/` with the `Goal` + `Streak` aggregates (`Entity/`), the `GoalType` + `Period` enums (`Enum/`), the `GoalTarget` VO (`ValueObject/`), the `GoalRepositoryInterface` + `StreakRepositoryInterface` ports (`Repository/`) and the `Goals*` deptrac layers. **Cross-module activity stream (HMAI-250):** the `ActivityProviderInterface` port (`Domain/Port/`) returns normalized `ActivityEvent` read models (`Domain/ReadModel/` — `GoalType` + `value` + `occurredAt`) for a `[from, to]` window; `Infrastructure/Activity/` holds four **DBAL read adapters** (Books/Series/Articles/YouTube) that read the source modules' tables (`book_reading_sessions`/`series_episodes` watched/`articles` read/`videos` watched) with raw SQL — importing **no** cross-module class, so deptrac stays 0/0 — plus a `CompositeActivityProvider` aggregating the tagged adapters (bound as the port in `services.yaml` via `!tagged_iterator goals.activity_provider`). **Goal definition — Application write side (HMAI-251):** the CQRS command side + persistence — `Application/Command/{CreateGoal,UpdateGoal,DeleteGoal}` + `Application/CommandHandler/*Handler` on `command.bus` (validation lives in the handlers: `GoalType`/`Period` via `tryFrom`→`InvalidArgumentException`, positive threshold via the `GoalTarget` VO, `GoalNotFoundException` on an update/delete miss; the goal's `type` is immutable so `UpdateGoal` only changes target+period), persisted by `DoctrineGoalRepository` (Doctrine ORM — `goals` table via `Goal.orm.xml` + the `GoalTarget` embeddable `target_value`, the enums mapped by the custom DBAL types `goal_type`/`goal_period` registered in `doctrine.yaml`; migration `Version20260710000001`), bound as `GoalRepositoryInterface` in `services.yaml`. **Progress & streak engine (HMAI-252):** the read/compute side — `Domain/Service/GoalProgressCalculator` (pure, no I/O, unit-tested) folds an `ActivityEvent` stream into the `Domain/ReadModel/{GoalProgress,StreakState}` read models: `progress()` sums the goal-type activity inside the current rolling window (`windowStartFor` — day / ISO-week-from-Monday / calendar-month, inclusive start), `streak()` computes the day-continuity run (distinct active days, same-day dedup; the current run counts only while the last active day is today or yesterday — a fully-missed day breaks it; the longest run is preserved). Query side `Application/Query/{GetGoalsProgress,GetStreaks}` + `Application/QueryHandler/*Handler` on `query.bus` (**DBAL** — read `goals` via `Connection`, pull the activity window via `ActivityProviderInterface`, fold with the calculator) returning `Application/DTO/{GoalProgressDTO,StreakDTO}` (streaks are per activity type, `GetStreaks` reads a 365-day lookback). **REST API (HMAI-253):** the thin `GoalsController` (`src/Controller/Api/`, `#[Route('/goals')]` — version-agnostic, served under `/api/v1/goals` + the `/api/goals` alias like every module, ADR-008): `GET /goals` (list-with-progress → `GetGoalsProgress`), `GET /goals/streaks` (→ `GetStreaks`), `POST /goals` (create → 201 `{id}`), `PUT /goals/{id}` (update target+period → 204), `DELETE /goals/{id}` (→ 204). Reads via `query.bus`, writes via `command.bus`; domain validation stays in the handlers — the controller only shape-checks the payload and maps `HandlerFailedException`→ `GoalNotFoundException` 404 / `InvalidArgumentException` 422. DTO→JSON via `GoalProgressDTONormalizer`/`StreakDTONormalizer` (`src/Serializer/`, HMAI-240; pinned in `NormalizersTest`). Documented with `#[OA\*]`+`#[Model]` and a `Goals` OpenAPI tag, so the contract still covers every `^/api/*` module. **Frontend (HMAI-254):** the `/goals` page (`app_frontend_goals` → `templates/goals/index.html.twig`, nav link in `base.html.twig`) driven by the Stimulus `goals_controller.js` (panel style, `apiCall` from `assets/util.js`): a new-goal form (type/period selects populated from the shared labels), a card grid with progress bars (`achieved/target` + capped percent + a met highlight), inline edit (target+period) and delete, plus a streak card per activity type. The pure presentation helpers (`typeLabel`/`periodLabel`/`clampPercent`/`progressText`/`streakLabel`/`longestLabel`) live in `assets/goals/format.js` — exported and Vitest-covered (`assets/tests/goals_format.test.js`); E2E `goals.desktop.spec.ts`/`goals.mobile.spec.ts`. **Background streak recalc (HMAI-255):** the `RecalculateStreaks` command (`Application/Command/`, no payload) + `RecalculateStreaksHandler` (`Application/CommandHandler/`, `#[AsMessageHandler(bus:'command.bus')]`, **routed async**) folds the activity stream (pulled via `ActivityProviderInterface` — no source-module coupling, deptrac 0/0) into a **persisted `Streak` per type** via the engine: `DoctrineStreakRepository` implements the `StreakRepositoryInterface` port over a new `streaks` table (`Streak.orm.xml` — `type` unique via `goal_type` DBAL type, `current_length`/`longest_length`/nullable `last_activity_date`; migration `Version20260710000002`), bound in `services.yaml`. Idempotent: deterministic recompute upserts one streak per type via the new `Streak::reconcile()` and preserves the **all-time longest** run (`max`) even when it falls outside the 365-day read window. Fired nightly by the Scheduler (`0 1 * * *`) so a midnight-broken streak is reflected without a read. NOTE: the read side `GetStreaks` (HMAI-252) still computes on-the-fly — the persisted streak is written but not yet read (a future optimization). **Test pyramid (HMAI-256):** the module's coverage is closed — `GoalsApiTest` (Integration) exercises the full HTTP read path with **seeded activity** (non-zero progress `achieved`/capped `percent`/`met`, and the day-continuity streak `currentLength`/`longestLength`/`lastActivityDate`) on top of the CRUD + 201/204/404/422 contract, complementing the handler-level `GoalsProgressQueryTest` + adapter-level `ActivityAdaptersTest`; the E2E `goals.{desktop,mobile}.spec.ts` cover the create-goal→see-progress+streak user flow on both viewports. The Goals module is now complete end-to-end (Domain→Application→Infrastructure→API→UI + background recalc), closing epic HMAI-248 (fixVersion 1.19.0, released 1.19.0). Dual-track frontend: the Series + Books + YouTubeProgress + Goals UI via Webpack Encore + Stimulus (`app/assets/`); Tasks/Articles/Music on Twig + vanilla JS (`app/public/js/`) with `window.apiCall` from `public/js/util.js`.) (+ **Search** — module complete, **released in 1.20.0** (2026-07-11), epic HMAI-265 — cross-module global search; MySQL FULLTEXT MVP, Elasticsearch deliberately out of scope → separate 1.30.0 epic HMAI-359). **Domain skeleton (HMAI-266):** `src/Module/Search/Domain/` with the `SearchResult` (type + id + title + snippet + url) and `SearchQuery` (term + optional `typeFilter` + 1-based `page`/`perPage`, `MAX_PER_PAGE=100`) value objects (`ValueObject/` — `final readonly`, invariant-guarded + `equals()`), the `SearchResultType` enum (`Enum/` — `article`/`book`/`series`/`music`/`task`, the normalized source-module + entity kind, à la `GoalType`), the `SearchEngineInterface` port (`Domain/Port/` — `search(SearchQuery): SearchResult[]`, to be backed by a DBAL/FULLTEXT adapter in HMAI-268), the placeholder `Application/`+`Infrastructure/` hex dirs (`.gitkeep`) and the `Search*` deptrac layers (Domain → Shared only, deptrac 0/0). **Cross-module indexing (HMAI-267):** the `SearchableProviderInterface` port (`Domain/Port/`) returns normalized `SearchableDocument` read models (`Domain/ReadModel/` — `type`+`id`+`title`+`content`+`url`); `Infrastructure/Provider/` holds five **DBAL read adapters** (Books/Articles/Series/Tasks/Music) reading the source tables (`books`/`articles`/`series`/`tasks`/`music_listening_sessions` — Music de-duplicating albums by artist+title with `GROUP BY`) with raw SQL, importing **no** cross-module class (deptrac 0/0), plus a `CompositeSearchableProvider` aggregating the tagged adapters (bound as the port in `services.yaml` via `!tagged_iterator search.searchable_provider`, à la Goals HMAI-250; the ticket's "via query.bus" wording is superseded by the deptrac-tight DBAL read). **Search engine (HMAI-268):** the read side is the `SearchEngineInterface` adapter `FulltextSearchEngine` (`Infrastructure/Engine/`) running `MATCH…AGAINST … IN NATURAL LANGUAGE MODE` over a Search-owned `search_documents` table (FULLTEXT index on `title`+`content`; migration `Version20260711000001`, excluded from `doctrine.dbal.schema_filter` as a non-ORM table) — relevance-ranked (`ORDER BY score`), paginated (`LIMIT/OFFSET`), optional type filter → `SearchResult[]`; served by `Application/Query/Search` + `SearchHandler` on `query.bus`. The index is fed by `SearchIndexer` (`Infrastructure/Index/`, behind the `SearchIndexerInterface` port) which rebuilds `search_documents` from the `CompositeSearchableProvider` in one transaction (idempotent); the `ReindexSearchDocuments` command (`command.bus`, **sync**) is fired by the Scheduler every 15 min (`*/15 * * * *`). The FULLTEXT engine sits behind the Domain port so an Elasticsearch swap (1.30.0/HMAI-359) needs no read-side change. **REST API (HMAI-269):** the thin `SearchController` (`src/Controller/Api/`, `#[Route('/search')]` — version-agnostic, served under `/api/v1/search` + the `/api/search` alias, ADR-008): `GET /search?q=&type=&page=&perPage=` builds a `SearchQuery` VO (422 via its `InvalidArgumentException` on a blank phrase / out-of-range page·perPage / unknown `type`), dispatches `Search` on `query.bus`, serializes `SearchResult[]` via `SearchResultDTONormalizer` (`src/Serializer/`, HMAI-240; pinned in `NormalizersTest`). Documented with `#[OA\*]`+`#[Model(SearchResult)]` under a new `Search` OpenAPI tag, so the contract still covers every `^/api/*` module. `SearchApiTest` (HTTP: ranked results, type filter, pagination, empty, 422/401, versioned+alias) + `SearchApiDocTest`. **Result cache (HMAI-271):** `CachingSearchEngine` (`Infrastructure/Cache/`) decorates the FULLTEXT engine behind the same port — caches `SearchResult[]` in a dedicated Redis pool `cache.search` (TTL 300s; `cache.adapter.array` in test) keyed by the normalized query (lowercased+trimmed phrase + type + page + perPage). Invalidation: `SearchIndexer.reindex()` clears the pool — the reindex is the "source data changed" signal, so a cached hit never outlives a reindex (no cross-module event coupling). **Frontend (HMAI-270):** a **global search bar** in the navbar (`templates/_search.html.twig`, included in `base.html.twig` → present on every page) driven by the Stimulus `search_controller.js`: a debounced (250 ms, min 2 chars) input → `apiCall('/api/search?q=…')` → results grouped by type in a dropdown with loading/empty/error states; each hit links to the source module via `safeUrl`. The pure helpers (`typeLabel`/`groupByType`) live in `assets/search/format.js` — exported + Vitest-covered (`assets/tests/search_format.test.js`); E2E `search.{desktop,mobile}.spec.ts`. **Test pyramid (HMAI-272):** the module's coverage is closed — `SearchCrossModuleApiTest` (Integration) seeds all five source-module tables, runs the **real** `SearchIndexer` (composite provider → index) and asserts over HTTP `/api/search` that a single query spans every module (5 distinct types), is relevance-ranked (the densest match first), type-filterable, paginated (non-overlapping pages) and empty-safe — proving the whole chain, complementing the direct-seed `SearchApiTest` (HTTP contract), the port-level `SearchIndexingTest` and the per-adapter `SearchableAdaptersTest`; the E2E `search.{desktop,mobile}.spec.ts` gain a phrase→results→**click-navigates-to-entity** flow on both viewports; and `OpenApiContractTest` now runtime-validates the `/api/search` response against its documented schema (closing the same gap Goals closed in its epic review, so every documented `^/api/*` module is under the conformance gate). The Search module is now complete end-to-end (Domain→Application→Infrastructure→API→UI + reindex/cache), closing epic HMAI-265 (fixVersion 1.20.0, released 1.20.0).) (+ **Dashboard** — module complete, **released in 1.21.0** (2026-07-14), epic **HMAI-257** / fixVersion **1.21.0** — a startup cockpit aggregating each module's "today" slice into one landing view (replaces the bare `/` redirect). **Aggregation port (HMAI-258):** `src/Module/Dashboard/Domain/Port/DashboardDataProviderInterface` is the cockpit's single read contract, returning normalized per-widget read models (`Domain/ReadModel/` — `TodayTask`, `DailyArticle`, `GoalSnapshot`, `Recommendation`, `RecentTrack`); `Infrastructure/Provider/` holds five **DBAL read adapters** (`TasksTodayAdapter` today's pending tasks / `DailyArticleAdapter` the day's article pick / `GoalsSnapshotAdapter` goals+persisted streaks / `RecommendationsAdapter` ongoing series + currently-read books / `RecentMusicAdapter` latest listening) reading the source modules' tables (`tasks`/`article_daily_picks`+`articles`/`goals`+`streaks`/`series`+`books`/`music_listening_sessions`) with raw SQL — importing **no** cross-module class, so deptrac stays 0/0 — plus a `CompositeDashboardDataProvider` delegating each fragment to its adapter, bound as the port in `services.yaml` (autowired). The `Dashboard{Domain,Application,Infrastructure}` deptrac layers were added. The ticket's "via query.bus" wording is superseded by the deptrac-tight DBAL read (the Goals HMAI-250 / Search HMAI-267 precedent). **Query layer (HMAI-259):** the CQRS read side — `Application/Query/GetDashboard` (carries the reference day, supplied by the controller as "today") + `GetDashboardHandler` on `query.bus` composes every widget through the port into one `Application/DTO/DashboardDTO` (sections: today's tasks, the daily article, goal snapshots, recommendations, recent tracks — the DTO composes the Domain read models directly, the `MusicComparisonDTO` precedent). The handler is **per-widget fault-isolated**: a widget whose source throws degrades to an empty/null section (logged `warning`) instead of failing the whole cockpit ("pusta sekcja zamiast błędu całości"). **REST API (HMAI-260):** the thin `DashboardController` (`src/Controller/Api/`, `#[Route('/dashboard')]` — version-agnostic, served under `/api/v1/dashboard` + the `/api/dashboard` alias, ADR-008): `GET /dashboard` dispatches `GetDashboard(today)` on `query.bus` and serializes the `DashboardDTO` via `DashboardDTONormalizer` (`src/Serializer/`, HMAI-240 — pure field mapping over the composed Domain read models, ISO-8601 datetimes; pinned in `NormalizersTest`) — no domain logic in the controller. Documented with `#[OA\Get]`+`#[Model(DashboardDTO)]` under a new `Dashboard` OpenAPI tag (the DTO's `list<>` widget-section phpdoc → clean array-of-model schemas), so the contract still covers every `^/api/*` module. `DashboardApiTest` (HTTP: full read-model / empty-state / versioned+alias / 401) + `DashboardApiDocTest`. **Home route (HMAI-261):** `/` now **renders the cockpit** instead of redirecting to a module — `FrontendController::index()` returns the `dashboard/index.html.twig` shell (route named `app_frontend_dashboard`; the navbar brand links here). Following the dual-track frontend pattern the page is a thin Twig shell — its `#dashboard-content` container is filled client-side from `/api/dashboard` (which goes through query.bus), so the controller stays thin with no bus injection; the widget layout + Stimulus `dashboard` controller arrive with HMAI-262. The old `→ /series` redirect was removed entirely (no fallback-redirect — empty widgets degrade per-section in the UI + the handler's fault isolation). `FrontendControllerTest` pins the cockpit render at `/` (200 HTML + `#dashboard-content`, no redirect) and the intact module navigation. **Frontend (HMAI-262):** the cockpit UI — the Stimulus `dashboard_controller.js` (`assets/controllers/`, auto-registered as `dashboard`) fetches `/api/dashboard` on connect and renders five widget cards into the `#dashboard-content` shell: today's tasks (time range + title), the daily article (link + category + read-time + read badge), goal snapshots (type + target·period + current streak), recommendations (series/book with cover + detail), and recent tracks (artist — title + source). Each widget degrades to a per-widget empty-state card (no data never breaks the grid), and a fetch failure shows a single error card. The pure presentation helpers (`goalTypeLabel`/`goalPeriodLabel`/`recommendationKindLabel`/`musicSourceLabel`/`emptyStateLabel`/`formatTime`/`formatTimeRange`/`readTimeLabel`/`streakLabel`) live in `assets/dashboard/format.js` — exported + Vitest-covered (`assets/tests/dashboard_format.test.js`). Widgets render client-side in JS (the Goals/Series precedent — no per-widget Twig partials); the `.dashboard-*` responsive card grid lives in `app.css`. The Dashboard UI joins the Encore + Stimulus dual track. **Read cache (HMAI-263):** `GetDashboardHandler` composes the cockpit behind the `DashboardCacheInterface` **Application port** (`Application/Cache/`) — the handler stays Infrastructure-agnostic (Application must not depend on Infrastructure). `RedisDashboardCache` (`Infrastructure/Cache/`) implements it over a dedicated Redis pool `cache.dashboard` (TTL 300s; `cache.adapter.array` in test), keyed per reference day (`dashboard_{Ymd}`) so a hit serves the whole composed `DashboardDTO` from Redis instead of re-running every adapter, and a new day starts fresh. Freshness is bounded by the short TTL + the day-scoped key; **cross-module event invalidation is deliberately NOT wired** — the Dashboard would have to import the source modules' event classes, breaking deptrac 0/0 (the Search HMAI-271 precedent — TTL is the staleness bound). The port's `invalidate(day)` stays available for a manual/ops clear. Bound in `services.yaml`; `RedisDashboardCacheTest` pins hit/miss/invalidation and `GetDashboardHandlerTest` pins that a second dispatch of the same day is served from cache. **Test pyramid (HMAI-264):** the module's coverage is closed — the Integration `DashboardApiTest` (HTTP: full read-model / empty widget sections / versioned+alias / 401, delivered with HMAI-260) is complemented by E2E `dashboard.{desktop,mobile}.spec.ts` covering the cockpit at `/` on both viewports: with a stubbed `/api/dashboard` all five widget cards render (task time-range/title, article link + read badge, goal type + streak, series/book recommendation, recent track + source label), an all-empty read-model degrades to five per-widget empty-state cards without breaking the grid, and the module navigation stays available from the home page. The Dashboard module is now complete end-to-end (Domain→Application→Infrastructure→API→UI + read cache), closing epic HMAI-257 (fixVersion 1.21.0). **Epic review (HMAI-257):** the module joins the **runtime conformance gate** — `OpenApiContractTest` now validates the `GET /api/v1/dashboard` response (a seeded, fully-populated read-model — task time range, daily article, a goal snapshot joined to a non-null persisted streak, an ongoing-series recommendation, a recent track) against its documented `DashboardDTO` schema, so **every documented `^/api/*` module is under the gate** (the same closeout Goals/Search did in their epic reviews). The gate surfaced one drift, fixed here: the goal snapshot's `lastActivityDate` is now serialized as **ISO-8601** (`DateTimeInterface::ATOM`) matching the `date-time` schema auto-derived from the `GoalSnapshot` read model's `?DateTimeImmutable`, uniform with the section's other datetime fields (`startsAt`/`endsAt`/`playedAt`) — `NormalizersTest` re-pinned accordingly. A Confluence module page was added.) (+ **Notifications** — module **in progress**, epic **HMAI-275** / fixVersion **1.24.0** — a proactive delivery layer (e-mail + WebPush) triggered both reactively (domain events via Messenger) and on a schedule, with per-type/per-channel opt-in and quiet hours. **Domain skeleton (HMAI-276):** `src/Module/Notifications/Domain/` with the `Notification` + `NotificationPreference` aggregates (`Entity/`), the `Channel` (`email`/`push`) + `NotificationType` (`task_due`/`article_daily`/`goal_streak_at_risk`/`daily_digest`) + `NotificationStatus` (`pending`/`sent`/`failed`) enums (`Enum/`), the `QuietHours` VO (`ValueObject/` — `fromTimes('HH:MM','HH:MM')`, start-inclusive/end-exclusive, `covers()` handling the overnight wrap 22:00–07:00; start must differ from end), the `NotificationRepositoryInterface` (incl. `findByDedupKey` — the idempotency lookup) + `NotificationPreferenceRepositoryInterface` ports (`Repository/`), the `NotificationChannelInterface` delivery port (`Port/` — `channel()` + `send()`, adapters report failure by throwing) and the `Notifications*` deptrac layers (deptrac 0/0). `Notification` carries the type/channel/payload/dedup key + delivery state, guarding its transitions (`markSent()` rejects a second send, `markFailed()` rejects a sent notification, a failed one may be retried); `NotificationPreference` records the per-type toggle, the enabled channels and an optional quiet-hours window. **Deliberately excluded from the skeleton:** the send/skip decision (whether the type is on, the channel is on, quiet hours block) stays for the dispatch engine (HMAI-278) — the aggregates only expose the primitives (`isEnabled`/`isChannelEnabled`/`quietHours()`), so the engine composes them. No Doctrine mapping/migration yet (persistence arrives with the write side), no Domain Events (YAGNI, the Goals precedent).) (+ **Movies** — module **in progress**, epic **HMAI-285** / fixVersion **1.23.0** — a films module alongside Series (CRUD, watched, metadata, list, Trakt import). **Domain skeleton (HMAI-286):** `src/Module/Movies/Domain/` with the flat `Movie` aggregate (`Entity/` — `id` + `Title` + `createdAt`, guarding a non-empty id, `rename()` its only transition), the `Title` VO (`ValueObject/` — `final readonly`, trimmed, non-empty, `MAX_LENGTH=255` measured in characters via `mb_strlen`, `equals()`), the `MovieRepositoryInterface` port (`Repository/` — save/findById/findAll/remove) and the `Movies*` deptrac layers (deptrac 0/0). A movie has no season/episode hierarchy, so the aggregate is flatter than `Series` — the pattern (aggregate + VO + port + Doctrine XML) is identical. **Persistence ships with the skeleton** (unlike the Notifications skeleton, which deferred it): `DoctrineMovieRepository` (`Infrastructure/Persistence/`, bound as the port in `services.yaml`) over the `movies` table — `Movie.orm.xml` + the `Title` embeddable mapped to the `title` column (`Title.orm.xml`, the Books `ISBN` precedent), both dirs registered in `doctrine.yaml` as `MoviesDomainEntities`/`MoviesDomainValueObjects`; migration `Version20260715000001` (no index yet — nothing queries `movies` until the CRUD/list tickets bring the query that would earn one). The mapping is **exercised**, not just `schema:validate`-checked: `MovieRepositoryTest` (Integration, the `GoalRepositoryTest` precedent) round-trips the `Title` embeddable through a real save→clear→find (incl. a 255-character multibyte title), because a mapping that hydrates wrong only blows up at read time — the hazard that forced the `series_rating` custom DBAL type. **Deliberately excluded from the skeleton:** catalog metadata, the `watched` flag and ratings (they arrive with the tickets that add their behavior), and — deviating from the ticket's hint — there is **no `MovieId` VO**: Doctrine cannot use an embeddable as an identifier, so an id VO would need a bespoke DBAL type that no other module has, while all ten aggregates key on a plain `string $id` and the read side is DBAL anyway. No Domain Events (YAGNI, the Goals/Notifications precedent). **Application write side — CRUD (HMAI-287):** the CQRS command side — `Application/Command/{AddMovie,UpdateMovie,DeleteMovie}` + `Application/CommandHandler/*Handler` on `command.bus`: `AddMovieHandler` mints a UUID v4 + stamps `createdAt` and returns the new id; `UpdateMovie` only renames (the `Title` is the sole mutable field the flat aggregate exposes, so — unlike `Series` — there is no separate rename/renumber split); `DeleteMovie` removes. Validation lives in the handlers — the non-empty/≤255 `Title` VO throws `InvalidArgumentException`, an update/delete miss throws `MovieNotFoundException` (`Application/Exception/`, `extends DomainException` — the `GoalNotFoundException` precedent). Persisted through the `MovieRepositoryInterface` port already bound to `DoctrineMovieRepository` — **no `services.yaml` change** (the binding shipped with the skeleton, the handlers auto-register via autowiring + `#[AsMessageHandler(bus:'command.bus')]`) and **no new migration** (schema unchanged from the skeleton). Handler-level unit tests pin create-returns-id / rename / not-found / empty-title-rejected (no-save on the failure paths). The REST/HTTP surface + list/filter UI arrive with HMAI-291.).

**Status:** operational project. Newest release **1.21.0** (2026-07-14) ships the **Dashboard** module (epic HMAI-257 — a startup cockpit aggregating each module's "today" slice into one landing view at `/`; see the Dashboard module entry above). Like 1.20.0 (Search) and 1.19.0 (Goals), it was built on top of 1.22.0, so `1.22.0` stays the **highest-numbered tag** on GitHub (the roadmap assigned Dashboard fixVersion 1.21.0, Search 1.20.0 and Goals 1.19.0 before the OpenAPI epic shipped as `1.22.0`). `1.22.0` is the API-contract release (epic **HMAI-311**: a formal, versioned OpenAPI 3.1 REST contract; 8 subtasks, each on its own green CI). **NelmioApiDocBundle** generates a machine-readable OpenAPI 3.1 document for the whole `^/api/*` surface from controller attributes — thin controllers and the hexagonal architecture untouched — with public Swagger UI (`/api/doc`), Redoc (`/api/doc/redoc`) and the raw spec (`/api/doc.json`) (HMAI-336). Shared contract components (`ApiKeyAuth` scheme, error schemas 401/404/409/422/429/500, pagination, rate-limit/`X-Request-ID` headers — HMAI-337); **API versioning `/api/v1`** with a backward-compatible `/api` alias (ADR-008, HMAI-338 — the six API controllers moved to `src/Controller/Api/` with version-agnostic routes imported twice); every module documented with `#[OA\*]`+`#[Model]` (Series+health HMAI-339, Tasks HMAI-340, Books+Articles HMAI-341, Music+YouTubeProgress HMAI-342 — 39 paths, 7 tags). The contract is a **CI quality gate** (5th job `openapi-contract`: `openapi.json` artifact + Spectral lint + `opis/json-schema` response↔schema conformance — HMAI-343). Foundation for the mobile client's stable API contract. The Dashboard module (1.21.0) adds a new bounded context on top of Search (1.20.0); **1199 PHP / 71 Playwright / 43 Newman / 64 Vitest JS** (+25 PHP vs 1174 at 1.20.0, from the Dashboard module); PHPStan level 8 clean, deptrac zero `skip_violations`. Previous: 1.20.0 Search (epic HMAI-265), 1.19.0 Goals (epic HMAI-248), 1.22.0 API contract (epic HMAI-311), 1.18.0 tech-debt (epic HMAI-232 — deptrac zero `skip_violations`, shared kernel, thin controllers, coverage gate 93.66%/floor 90, Symfony 8.0→8.1), 1.17.0 runtime/infra maintenance, 1.16.0 Books+Music GUI, 1.15.0 Articles GUI, 1.14.0 Tasks GUI. Six domain modules in production. Full history → [CHANGELOG.md](CHANGELOG.md). Follow-up **HMAI-274 done**: phpstan-baseline reduced to a single deliberately-kept entry — the ~26 residual typing suppressions were fixed at the source (not re-baselined), leaving only the `ApiKeyAuthTest` wire-contract tautology.

## Architecture — INVIOLABLE RULES

- Hexagonal: `src/Module/{Name}/{Domain,Application,Infrastructure}/`
- **Shared kernel (HMAI-235):** cross-cutting domain value objects AND stable contracts that legitimately belong to more than one bounded context live under `src/Shared/` (`App\Shared\…`), NOT duplicated per module. Currently: the `CoverUrl` value object (Books + Series, `Shared/Domain/ValueObject/`), the `TokenCipherInterface` encryption port (Music + Tasks + Series Infrastructure, `Shared/Security/`, HMAI-234 — `App\Security\TokenCipher` wired as its implementation via DI), and the `GoogleTokenProviderInterface` read-only Google-token port (`Shared/Security/`, HMAI-237 — the YouTube adapter reads the OAuth token through it instead of reaching into Tasks Infrastructure; Tasks' `GoogleTokenRepositoryInterface` **extends** it for writes, and `GoogleOAuthTokenRepository` is wired as its implementation via DI). Deptrac has a dedicated `Shared` layer that every `*Domain` may depend on — and Infrastructure/Glue too — and it has zero outbound deps itself. Use it ONLY for genuinely shared, stable domain primitives/contracts — module-specific types stay in the module. The default is still "no cross-module coupling"; the shared kernel is the one sanctioned exception.
- Domain free of framework: `grep -r "use Doctrine" src/Module/*/Domain/` MUST return an empty result. CI gate: `make deptrac` — Domain → [Shared] only (zero framework/cross-module), cross-module coupling forbidden
- **Domain ports return Domain read models, never Application DTOs (HMAI-233):** the external-data ports (`BookMetadataProviderInterface`, `MusicListeningHistoryInterface`, `VinylCollectionInterface`, `YouTubePlaylistReaderInterface`) return per-module `final readonly` read models living in `Domain/ReadModel/` (`Books\…\BookMetadata`; `Music\…\{Album,RecentTrack,VinylRecord}`; `YouTubeProgress\…\VideoMetadata`) — plain immutable shapes, NOT `Application\DTO\*`. This removes the Domain→Application dependency the ports used to carry: deptrac no longer needs the four grandfathered `skip_violations` (and after HMAI-234 dropped the three `TokenCipher` entries and HMAI-237 the last YouTube→Tasks Google-token entry, deptrac now runs with **zero** `skip_violations`). The Infrastructure adapters build/cache them, the Glue `Album`/`VinylRecord` normalizers serialize them, and `MusicComparisonDTO` (Application) legitimately composes them (Application→Domain is allowed). A read model captured by `directory: src/Module/{Module}/Domain/.*` is in the Domain layer; using `DateTimeImmutable` in one is deptrac-clean (the global class is an uncovered token, not a violation)
- Doctrine XML in `Infrastructure/Persistence/Doctrine/*.orm.xml` — do NOT migrate to PHP attributes (ADR-001)
- Domain Events: the aggregate collects them in `$recordedEvents`, the handler dispatches after `releaseEvents()`. Pattern: the `Series` aggregate
- Query handlers: DBAL, NOT ORM (do not hydrate aggregates for reads)
- **API serialization (HMAI-240):** DTO→JSON goes through Symfony Serializer Normalizers in `src/Serializer/*DTONormalizer.php` (`App\Serializer\`, Glue layer) — controllers inject `NormalizerInterface` and return `$this->normalizer->normalize($dtoOrList)`, NOT hand-rolled private `serialize*` methods. One normalizer per top-level read DTO (auto-tagged `serializer.normalizer` via autoconfigure; `getSupportedTypes` keyed by the DTO class); nested DTOs delegate through `NormalizerAwareTrait` (`BookDetailDTO`→`BookDTO`, `WatchSessionDTO`→`VideoDTO`) instead of duplicating field maps. The Music `AlbumDTONormalizer`/`VinylRecordDTONormalizer` keep their class names but key on the **Domain read models** `Album`/`VinylRecord` (HMAI-233), not Application DTOs. The `Serializer` is `enabled` implicitly (symfony/serializer installed, no extra config). Regression: `tests/Unit/Serializer/NormalizersTest.php` (11 tests) + every module's `*ApiTest` pins the byte-identical JSON contract. The `SeriesDetailDTONormalizer` is **pure field mapping**: its per-show/per-season `averageRating`/`watchedCount`/`episodeCount` are computed upstream in the read layer (`SeriesRowHydrator`), NOT at serialize time (HMAI-242 — see the Series read-model bullet)
- Command handler: `#[AsMessageHandler(bus: 'command.bus')]`
- Query handler: `#[AsMessageHandler(bus: 'query.bus')]`
- Event handler: `#[AsMessageHandler]` without `bus:` (default)
- `event.bus` configured with `allow_no_handlers: true` — domain events are fire-and-forget, the subscriber is optional
- **Bus dispatch in controllers goes through the typed wrappers `App\Messaging\{QueryBus,CommandBus}` (HMAI-241), NOT the raw `MessageBusInterface`:** both use Symfony `HandleTrait`. `QueryBus::ask($q)` and `CommandBus::dispatchAndReturn($cmd)` return the single handler's result (they throw `LogicException` when no handler ran instead of dereferencing `null` — eliminating the null-unsafe chain `->dispatch(...)->last(HandledStamp::class)->getResult()`). `CommandBus::dispatch($cmd, $stamps=[])` is a fire-and-forget passthrough — **for async-routed commands** (e.g. `ImportWatchedShowsFromTrakt`, `RefreshDiscogsCollection`) it MUST go this way, because they get a `SentStamp` not a `HandledStamp` (they would throw through `HandleTrait`). Autowiring: `CommandBus` → default bus, `QueryBus` via `#[Target('query.bus')]` in the wrapper's constructor. Regression: `tests/Unit/Messaging/{QueryBus,CommandBus}Test.php`

## Naming conventions

| Element | Pattern | Location |
|---|---|---|
| Aggregate Root | `Series`, `Task`, `Book`, `Article` | `Domain/Entity/` |
| Value Object (immutable, `final readonly`) | `Rating`, `ISBN`, `CoverUrl`, `TimeSlot`, `ReadingProgress` | `Domain/ValueObject/` |
| Read Model (Domain port return, `final readonly`) | `BookMetadata`, `Album`, `RecentTrack`, `VinylRecord`, `VideoMetadata` | `Domain/ReadModel/` |
| Command | `CreateSeries`, `LogReadingSession` | `Application/Command/` |
| Command Handler | `*Handler` | `Application/Handler/` |
| Query | `GetAllSeries`, `GetSeriesDetail` | `Application/Query/` |
| Query Handler | `*Handler` | `Application/QueryHandler/` |
| DTO | `*DTO` | `Application/DTO/` |
| Repository Interface | `*RepositoryInterface` | `Domain/Repository/` |
| Repository Impl | `Doctrine*Repository` | `Infrastructure/Persistence/` |
| Serializer Normalizer | `*DTONormalizer` | `src/Serializer/` (Glue) |

## Frontend

- **Series + Books + YouTubeProgress UI:** Webpack Encore + Stimulus. Stimulus controllers in `assets/controllers/{series,books,youtube_progress}_controller.js`, mounted via `data-controller="..."` on `app/templates/{series,books,youtube_progress}/index.html.twig`. Build: `make assets-prod` → `public/build/*.{js,css}` + `entrypoints.json` manifest. `base.html.twig` uses `{{ encore_entry_link_tags('app') }}` + `{{ encore_entry_script_tags('app') }}`.
- **Series UI module split (HMAI-243):** the former 976-line `series_controller.js` god-controller is broken into ES modules under `app/assets/series/`: `api.js` (the per-endpoint HTTP layer), `banners.js` (`showError`/`hideError`/`showInfo`/`showTraktConnectPrompt` — controller-independent, they target the **global** `#error-banner`/`#info-banner` in `base.html.twig`), `ratings.js` + `list.js` (the **pure** helpers — `avg`/`cardRating`/`statusLabel`/`ratingHighlight`/`ratingFlag`/`cardRatingFlag` and `filterSeries`/`sortSeries`), and the DOM builders `rating-controls.js`/`inline-edit.js`/`list-view.js`/`season-view.js`/`detail-view.js`. The Stimulus controller (`controllers/series_controller.js`, ~180 lines) is now thin: lifecycle wiring + list/detail orchestration + the add-series modal, importing the modules. Pattern for the split: builders import the banner + pure helpers + `API` directly and take **navigation as callbacks** (`renderDetail(container, series, {reloadDetail, backToList})`, `renderSeriesList(container, list, onSelect)`) — no `this`/controller is threaded into the modules. The new modules live OUTSIDE `controllers/` so `bootstrap.js`'s `webpackContext` does not auto-mount them as controllers. No single Series-UI file > ~260 lines. Pure helpers stay unit-tested (`assets/tests/series_view.test.js`, imports repointed to `series/ratings.js`+`series/list.js`); `readMetadataInputs` is now covered too (`assets/tests/series_metadata.test.js`, jsdom); behavior pinned by `series.desktop.spec.ts`/`series.mobile.spec.ts`.
- **The remaining modules** (Tasks/Articles/Music): Twig + vanilla JS in `public/js/*.js`, global helpers `window.TOAST_TIMEOUT_MS` / `window.safeUrl` / `window.apiCall` from `public/js/util.js`.
- Routes: `/` → **cockpit** (the Dashboard home page, `app_frontend_dashboard`, HMAI-261 — no longer a redirect), `/series`, `/tasks`, `/books`, `/articles`, `/music`, `/youtube-progress`, `/goals`
- YouTubeProgress panel (`/youtube-progress`): `YouTubeProgressController` (`^/api/youtube-progress/*`) — `GET watchlist` + `GET sessions` read through `query.bus` (`GetWatchlist`/`GetSessions` → DBAL query handlers returning `VideoDTO`/`WatchSessionDTO`, HMAI-236; the controller no longer injects Domain repos for reads, consistent with the other modules); `POST sync` (dispatch `SyncWatchlist`+`RegenerateSessions`, 400 when `YOUTUBE_WATCHLIST_PLAYLIST_ID` is empty), `POST videos/{id}/start|watched`, `POST sessions/{id}/push-to-youtube` dispatch command handlers (404/idempotency in the handlers, unwrap via `ApiExceptionListener`). The Twig page is routed from `FrontendController` like the rest of the nav.
- Series rating selector: 10 buttons (NOT `<input type=number>`)
- Series — own rating: `Series` and `Season` have their **own, optional `?Rating`** (VO → `rating_value` column on `series` and `series_seasons`, nullable; the same pattern as `Episode`), independent of the average from episodes. **Mapping `?Rating` via the custom DBAL type `series_rating` (`Infrastructure/.../Doctrine/Type/RatingType.php`, the `SeriesStatusType` pattern), NOT an embeddable:** a nullable embeddable hydrates as a **non-null** object with an uninitialized `$value` when the column is NULL — harmless for the write path (set-only) and read path (DBAL), but it blows up every read of a hydrated VO (importing ratings from Trakt). The custom type round-trips `null` cleanly both ways, so `Series/Season/Episode::rating()` returns a real `null`. Aggregate: `Series::rate()` / `Series::rateSeason()` (delegates to `Season::rate()`) + clearing `Series::clearRating()` / `Series::clearSeasonRating()` (delegates to `Season::clearRating()`) — no Domain Event (no subscriber, YAGNI). Commands `RateSeries`/`RateSeason` on `command.bus` (field `?int $rating` — `null` = clear). Endpoints `PATCH /api/series/{id}/rating` and `PATCH /api/series/{seriesId}/seasons/{seasonId}/rating` (body `{rating:1..10}` → 204 sets it; `{rating:null}` → 204 clears the own rating; 422 out of range OR missing `rating` key — `SeriesRequestParser::parseNullableRating` distinguishes an explicit `null` from absence via `array_key_exists`; 404 not found). `GET /api/series/{id}` returns, for the show and the season, the disjoint `rating` (own) AND `averageRating` (computed from episodes). "My rating" controls in the show and season header (`series_controller.js`, reuse `renderRatingSelector`); when a rating is set, the "✕" button clears it (PATCH `{rating:null}`)
- Series — episode watched: `Episode` has a `watched` flag (bool, NOT NULL DEFAULT 0) + nullable `watchedAt` (`datetime_immutable`) — the `watched`/`watched_at` columns on `series_episodes` (the mapping declares the same `default: 0`, without which `schema:validate` drifts and raw-insert tests fail on "no default value"). Aggregate: `Series::setEpisodeWatched()` (delegates to `Episode::markWatched(?$watchedAt)` / `unmarkWatched()`) — no Domain Event (YAGNI), the optional `?$watchedAt` leaves a door open for the Trakt import to use a real date. Command `SetEpisodeWatched` on `command.bus`. Endpoint `PATCH /api/series/{seriesId}/seasons/{seasonId}/episodes/{episodeId}/watched` (body `{watched:bool}` → 204, 422 when non-bool, 404 when the show/season/episode is missing). `GET /api/series/{id}` returns per-episode `watched`/`watchedAt` plus `watchedCount`/`episodeCount` counters at the season and show level. UI: a "Watched" column (checkbox `.js-episode-watched`) + an `x/y watched` counter in the season header (`series_controller.js`)
- Series — deletion: `DELETE /api/series/{id}` (cascades seasons+episodes), `DELETE /api/series/{seriesId}/seasons/{seasonId}` (cascades episodes), `DELETE /api/series/{seriesId}/seasons/{seasonId}/episodes/{episodeId}` — all 204, 404 when missing. Commands `DeleteSeries`/`DeleteSeason`/`DeleteEpisode` on `command.bus`. Aggregate: `Series::removeSeason()` / `Series::removeEpisode()` (delegates to `Season::removeEpisode()`) return the removed entity and throw a `DomainException` (→404) when missing. **Cascade is explicit in the repo** (`SeriesRepositoryInterface::delete/deleteSeason/deleteEpisode` → `EntityManager::remove` + flush) — the aggregate has NO ORM associations (entities persisted manually via string FKs), so `orphanRemoval`/`cascade` will not work. This is a **deliberate design, formally closed as ADR-007** (HMAI-238 — manual string-FK persistence stays; ORM `OneToMany` associations were rejected to keep the N+1-free bulk loader in `attachSeasonsAndEpisodes()` and to avoid the nullable-VO hydration hazard that already forced the `series_rating` custom DBAL type; reads are DBAL anyway, so associations would only serve the write/delete path — the manual cascade is cheaper). The hand-written cascade is pinned by `SeriesRepositoryTest` (`delete`/`deleteSeason`/`deleteEpisode` asserted against raw row counts, so a future delete path that forgets to remove children fails loudly). The handlers invalidate Redis `series:avg:{id}` / `season:avg:{id}` after deletion. UI: trash buttons (🗑) in the show, season, and episode-row header with `confirm()` (`series_controller.js`)
- Series — editing: `PATCH /api/series/{id}` (body `{title}`), `PATCH /api/series/{seriesId}/seasons/{seasonId}` (body `{number}`), `PATCH /api/series/{seriesId}/seasons/{seasonId}/episodes/{episodeId}` (body `{title}`) — all 204, 422 (empty/`>255` title or number `<1`/non-int), 404 not found. **Renumbering a season to a number already used in the show → 409 Conflict** (dedicated `Domain\Exception\SeasonNumberAlreadyTaken extends DomainException`; the controller checks it BEFORE the generic `DomainException`, since it extends it). Commands `RenameSeries`/`RenumberSeason`/`RenameEpisode` on `command.bus`; aggregate `Series::rename()`/`renumberSeason()`/`renameEpisode()` (`Season::renumber()`, `Episode::rename()`) — the `title`/`number` fields stopped being `readonly`, persistence via a plain `save()` (Doctrine UoW change-tracking of managed entities, no new repo method). Shared title validation: `SeriesRequestParser::parseTitle()` (reused in create/addEpisode/rename). UI: inline-edit (click the title/number → input, Enter/blur saves, Esc cancels) — `buildInlineEditable()` in `series_controller.js`
- Series — thin controller / request parsing (HMAI-239): all payload parsing+validation lives in the stateless Glue service `App\Controller\Series\SeriesRequestParser` (NOT inline in `SeriesController`) — `decode`/`parseTitle`/`parseSeasonNumber`/`parseEpisodeNumber`/`parseOptionalEpisodeRating`/`parseRequiredRating`/`parseWatched`/`parseNullableRating`/`parseMetadata` (the latter returns the `SeriesMetadataInput` readonly bag with a `hasAnyField` flag for the PATCH partial-safe path). **On invalid input the parser throws `UnprocessableEntityHttpException`**, which `ApiExceptionListener` turns into the exact same `{"error": …}` 422 the controller used to hand-build — so the API contract (the 422 messages) is byte-for-byte unchanged. Controller actions are now decode→parse→dispatch→respond; the only logic left in the controller is the `HandlerFailedException`→404/409/422 domain-exception mapping (`mapDeleteFailure`/`mapRatingFailure` + inline catches) — read-model serialization moved out entirely (DTO→JSON via `SeriesDetailDTONormalizer`, HMAI-240; the `averageRating`/`watchedCount`/`episodeCount` computation into the `SeriesRowHydrator` read layer, HMAI-242). Validation rules are unit-tested in isolation: `tests/Unit/Controller/Series/SeriesRequestParserTest.php` (35 tests — title ≤255, year `[1900, year+5]`, status enum, description ≤2000, ratings, watched, positive ints, metadata presence flag)
- Series — catalog metadata: `Series` gains 4 **optional** fields: `?string $coverUrl` (the `cover_url` column, validated by the shared VO `App\Shared\Domain\ValueObject\CoverUrl` — http/https + `FILTER_VALIDATE_URL`; promoted to the Shared kernel in HMAI-235, previously a per-module copy in Books + Series), `?int $year`, `?SeriesStatus $status` (enum `Domain\Enum\SeriesStatus` = `ongoing`/`ended`, the `status` column via the custom DBAL type `series_status` in `Infrastructure/.../Doctrine/Type/SeriesStatusType.php` — the `BookStatusType` pattern, nullable round-trip; registered in `doctrine.yaml`), `?string $description` (the `description` column LONGTEXT = Doctrine `text`). Migration `Version20260613000001` (4 nullable columns). Aggregate: `Series::updateMetadata(?coverUrl, ?year, ?status, ?description)` = **full replace** (each field overwritten, `null` clears) — no Domain Event. `POST /api/series` and `PATCH /api/series/{id}` accept `{coverUrl, year, status, description}` (camelCase); validation in `SeriesRequestParser::parseMetadata()` (coverUrl via the VO → 422; year `int` in `[1900, year+5]`; status via `SeriesStatus::tryFrom`; description ≤2000 characters). **PATCH = partial-safe**: title still via `RenameSeries`, metadata dispatches `UpdateSeriesMetadata` **only when ≥1 metadata key is present** (`array_key_exists`) — a bare `{title}` (inline edit) does NOT zero out the metadata; the full edit form sends all 4. Command `UpdateSeriesMetadata` on `command.bus` (404 when the show is missing). `GET /api/series` + `/{id}` return `coverUrl`/`year`/`status`/`description`. UI: fields in the "New Series" modal + a "✎ Edit details" form in the detail (`buildEditDetailsForm`/`readMetadataInputs`), a poster thumbnail (2:3) on the list card + poster/year/status/description in the detail header (`series_controller.js`). Regression: `Unit/.../ValueObject/CoverUrlTest`, `SeriesAggregateTest::testUpdateMetadata*`, `SeriesApiTest` (create+GET metadata, PATCH update/clear/partial-safe, 422 for a bad coverUrl/year/status)
- Series — real episode number: `Episode` has `int $number` (the `number` column on `series_episodes`, NOT NULL, no default) — **unique within a season**, validated in `Season::addEpisode()` (throws `InvalidArgumentException` → 422 on a duplicate; allows re-adding the same id = replacement). `POST /api/series/{s}/seasons/{se}/episodes` now requires `{number:int≥1, title, rating?}` (422 for non-int/`<1`/duplicate). `GET` returns `number` per episode; reads sort `ORDER BY e.number` (query handlers), the UI renders `ep.number` (not the loop index) and sorts by number. Migration `Version20260612000004` backfills existing episodes with `ROW_NUMBER() OVER (PARTITION BY season_id ORDER BY id)` (= the previous UI order), then `MODIFY ... NOT NULL`. **Raw-insert tests into `series_episodes` must provide `number`** (NOT NULL with no default — `EpisodeRatedHandlerTest`). The add-episode form has a number field (default = max+1)
- Series — list: search + sorting + own rating on the card (**frontend-only** — `GET /api/series` already returns the full set): the `#series-toolbar` toolbar (input `#series-search` + select `#series-sort`) in `series/index.html.twig`, hidden when there are zero shows. `series_controller.js` keeps the full list in `this.allSeries`, `applyListView()` filters by title (case-insensitive, live) and sorts via `sortSeries()` (keys: `title` A–Z / `rating-desc` average ↓ / `own-desc` own ↓ / `created-desc` newest — `null` ratings at the end). An empty list vs no matches are two different states ("No series yet" vs "No series match your search"). The card shows the disjoint `cardRating('My', rating)` + `cardRating('Avg', averageRating)`. E2E: `series.desktop.spec.ts` (the filter narrows, `created-desc` changes the order)
- Series — read-model fields (HMAI-242): the per-show/per-season `averageRating` (mean of the episodes' own ratings, `round(…,2)`, `null` when none rated — disjoint from the manual `rating`), `watchedCount` and `episodeCount` are computed in the **read layer** — `SeriesRowHydrator` (Application, shared by `GetAllSeriesHandler`+`GetSeriesDetailHandler`), carried on `SeriesDetailDTO`/`SeasonDTO`, and merely copied by `SeriesDetailDTONormalizer` (pure mapping). NOT computed at serialize time (was the normalizer, before that `SeriesController::serializeDTO`). Regression: `SeriesRowHydratorTest` (avg rounding / multi-season span / null-when-unrated / counters) + `NormalizersTest` (pure pass-through) + `SeriesApiTest` (byte-identical JSON)
- Series — coloring of cards/seasons by rating (**frontend-only** — the read layer returns per-show/per-season `rating`/`averageRating`/`watchedCount`/`episodeCount`, see the read-model bullet above): a subtle card background on the list and on the show/season headers in the detail. Helpers in `series_controller.js`: `ratingHighlight(entity)` returns `'incomplete'|'mismatch'|null` (**`incomplete` = `episodeCount>0 && watchedCount<episodeCount`** has **priority** over `mismatch`, because the average of a partially watched show is unreliable; `mismatch` = `Math.round(averageRating) !== rating`, and also "there is an average, no own rating"), `ratingFlag()` → `{cls,title}` for the show/season headers, `cardRatingFlag(s)` → card: amber when the show is incomplete, otherwise red when the show **OR any season** has a `mismatch`. CSS classes `.is-rating-incomplete` (amber `#fff8e1`) / `.is-rating-mismatch` (red `#fdecea`) in `app.css` (compounded with `.series-card`/`.season-header`/`.series-detail-header` for specificity) + a `title` tooltip (a11y — color is not the only carrier). E2E: `series.desktop.spec.ts` (list stub → mismatch=red / incomplete=amber with priority / aligned=neutral)
- Series — import from Trakt (GUI): `POST /api/series/import/trakt` dispatches `ImportWatchedShowsFromTrakt` (async) → **202** `{status:"import_started"}` immediately (the import is rate-limited + I/O bound, it NEVER blocks the request — the same pattern as `RefreshDiscogsCollection`). **No Trakt token → 409** `{error, authUrl:"/auth/trakt"}` BEFORE the dispatch (`SeriesController::isTraktConnected()` reads `TraktTokenRepositoryInterface::get()` — the token itself, no network I/O; refresh-on-expiry is done by the worker). UI: an "Import from Trakt" button in the Series header (`#btn-import-trakt`, `series_controller.js`) → `API.importFromTrakt()`; 202 shows `#info-banner` ("import started…", auto-hide), 409 sets `#error-banner` with a `/auth/trakt` link (no auto-hide). The `#info-banner` banner is in `base.html.twig` (green, `.info-banner` in `app.css`). Regression: `ImportFromTraktApiTest` (409 not-connected with no dispatch / 202 + dispatch to the async transport), E2E `series.desktop.spec.ts` (202 stub → info banner / 409 stub → connect link)
- Tasks API: full REST CRUD (`POST/GET/GET{id}/PATCH{id}/DELETE{id} /api/tasks`, `POST {id}/complete`, `POST {id}/cancel`) + `/time-report` + `/export`. Google Calendar sync via `CalendarServiceInterface` with graceful degrade

### Webpack Encore

| File | Role |
|---|---|
| `app/webpack.config.js` | Encore 7 config (**ESM**: `import Encore`, top-level `await Encore.getWebpackConfig()`, `"type":"module"` in `package.json`) — entry `app`, splitEntryChunks, Stimulus bridge, versioning prod-only, `configureBabel` with `polyfill-corejs3` |
| `app/assets/app.js` | Main entry — imports `bootstrap.js` (Stimulus) + `styles/app.css` |
| `app/assets/bootstrap.js` | `startStimulusApp` auto-discovery from `controllers/` via **`import.meta.webpackContext`** (ESM-native; NOT `require.context` — see below) |
| `app/assets/util.js` | ES module export: `TOAST_TIMEOUT_MS`, `safeUrl`, `apiCall`, `escHtml` |
| `app/assets/controllers/series_controller.js` | Thin Stimulus controller for the Series UI (~180 lines): lifecycle wiring + list/detail orchestration + the add-series modal. The view logic + pure helpers were extracted to `app/assets/series/*.js` (HMAI-243) |
| `app/assets/series/*.js` | Series UI ES modules (HMAI-243): `api`/`banners`/`ratings`/`list`/`rating-controls`/`inline-edit`/`list-view`/`season-view`/`detail-view`. Pure helpers in `ratings.js`+`list.js`; DOM builders elsewhere; imported by the controller. NOT under `controllers/` so `bootstrap.js`'s `webpackContext` does not register them as Stimulus controllers |
| `app/assets/controllers/books_controller.js` | Stimulus controller for the Books UI |
| `app/assets/controllers/youtube_progress_controller.js` | Stimulus controller for the YouTubeProgress panel (sync/start/watched/push) |
| `app/assets/controllers/dashboard_controller.js` | Stimulus controller for the Dashboard cockpit (HMAI-262) — fetches `/api/dashboard` and renders the five widget cards into `#dashboard-content` |
| `app/assets/dashboard/format.js` | Pure Dashboard presentation helpers (labels / time / empty-state); Vitest-covered, imported by the controller. NOT under `controllers/` so `bootstrap.js`'s `webpackContext` does not register it as a Stimulus controller |
| `app/assets/styles/app.css` | Global stylesheet (the single source of truth) |
| `app/vitest.config.js` | Vitest config (ESM) — `environment: 'jsdom'` (needed by `safeUrl`/`URL`), `include: ['assets/**/*.test.js']` |
| `app/assets/tests/*.test.js` | JS unit tests (Vitest). Kept OUT of `assets/controllers/` because `bootstrap.js`'s `webpackContext` auto-mounts **every** `.js` there as a Stimulus controller (a test file under `controllers/` would break the boot/build) |

Commands: `make assets` (dev), `make assets-watch` (watch mode), `make assets-prod` (production), `make node-audit` (CVE gate), `make test-js` (Vitest JS unit tests). Node service: `aihm-node-1` (`node:24-alpine`, mounted on `./app`). `make node-install` re-runs `npm install` after a `package.json` change.

`public/build/` + `node_modules/` in `.gitignore`. CI builds the assets in the `tests` and `e2e-playwright` jobs (`npm ci && npm run build` in `app/`) before PHPUnit/Playwright — without this Twig `encore_entry_*` throws 500.

**Encore 7 + Babel 8 + webpack-cli 7 (ESM, HMAI-226):** `webpack.config.js` is **ESM** (`import Encore`, top-level `await Encore.getWebpackConfig()`, `"type":"module"` in `package.json`). `@symfony/webpack-encore@7.1` declares `peer webpack-cli@^6 || ^7` — `webpack-cli` is pinned to 7.x. Babel 8 (`@babel/core`/`@babel/preset-env` 8.x) removed the `useBuiltIns`/`corejs` options from `preset-env`; core-js polyfills are now injected by `babel-plugin-polyfill-corejs3` via `configureBabel` (push `['polyfill-corejs3',{method:'usage-global',version:'3.38'}]`). The polyfill targets go through `"browserslist": ["defaults"]` in `package.json` (Encore sets preset-env `targets:{}` → falls back to browserslist; the polyfill plugin reads the same browserslist) — `defaults` (2026 baseline) natively supports `at`/`replaceAll`/`fromEntries`/`allSettled`, so our modern code gets **0 polyfills**. **Stimulus bootstrap under ESM (GOTCHA):** `assets/bootstrap.js` loads controllers via `import.meta.webpackContext` (**NOT** `require.context`) — `"type":"module"` makes webpack parse the asset `.js` files as `javascript/esm`, where the CommonJS `require.context` stays a raw `require` reference → runtime `ReferenceError: require is not defined` at startup, dead Stimulus (zero controllers mount). The build **compiles anyway** (a free var ≠ a compile error), so the bug is only caught by E2E in the browser (a green `npm run build` alone is not enough — you need a real page boot). `import.meta.webpackContext` is the webpack-5 ESM-native equivalent that the parser transforms in an ESM module. Three `ignore` rules in `.github/dependabot.yml` (`webpack-cli` + `@babel/core` + `@babel/preset-env`) were **removed** — Encore 7 covers those majors with its peer range, so Dependabot refreshes them again. **Caveat for the future:** another major of an Encore peer outside its range (e.g. `webpack-cli` 8 outside `^6||^7`) will again break `npm ci` with `ERESOLVE` — do not merge a peer major before Encore widens its peer range (precedent: the ill-fated auto-merge of webpack-cli 7 / Babel 8 on Encore 6 → hotfixes PR #212/#231).

**Symfony 8.1 bump (HMAI-224 → HMAI-225, RESOLVED):** Symfony was deliberately held at 8.0.\* through 1.17.0 because `symfony/framework-bundle 8.1.0` had a regression — the resolved config `event.bus → default_middleware.allow_no_handlers: true` did NOT reach the compiled `HandleMessageMiddleware` (the value landed in the wrong constructor argument after the 8.1 signature change; `allowNoHandlers` stayed `false`), throwing `NoHandlerForMessageException` on every handler-less fire-and-forget domain event (`TaskCreated/Updated/Completed/Cancelled/Deleted`) → 500 → 11 red Tasks tests. **`framework-bundle 8.1.1` fixes it.** The whole Symfony group is now on **8.1.\*** (Dependabot-bumped on master, back-merged to develop); verified green on 8.1.1 — 977 PHPUnit + deptrac + PHPStan level 8. HMAI-225 is effectively closed.

(`newman 7` still does not exist — latest is 6.2.2 — the root stays on newman 6.x; see the section below.)

**npm audit gate:** every frontend `npm ci` of deps (`tests` job + `e2e-playwright`, both in `app/`) is immediately followed by `npm audit --audit-level=high`. Low/moderate are noise for devDeps and are let through; high+critical block the merge. Fix = bump the package (`npm install pkg@latest`), not suppress — an advisory on the installed version is a legit signal. Locally: `make node-audit`.

The root `package.json` (Playwright + Newman) is **deliberately outside the gate**: newman 6.x (latest stable) pulls a deep-transitive CVE in `handlebars`/`lodash`/`postman-*` with no forward fix from the vendor; `audit fix --force` would roll back to newman 2.1.2 and break the Postman collection. Re-evaluate when newman 7.x ships with a clean dependency tree — then the gate returns to the root.

## Infrastructure

| Service | Container / Port | Notes |
|---|---|---|
| MySQL 8.4 LTS | `mysql:3306` | Image pinned to `mysql:8.4` (the LTS line) in compose **and** CI (×3 jobs) — NOT the floating `mysql:8` (its tag currently still resolves to 8.4.9, but after 8.0 EOL it would jump to 9.x innovation; the pin = reproducibility dev↔CI↔prod). DB `homemanager`. **`serverVersion=8.0` in the DSN stays deliberately** (NOT 8.4): DBAL 4.4 for `8.4` picks `MySQL84Platform`, which is `@deprecated` and differs from `MySQL80Platform` **only** in the reserved-keyword list (zero changes in schema SQL generation) — the 8.0 platform is fully compatible with the 8.4 server, `schema:validate` with no drift. We stay on **8.4 LTS, not 9.x** (9.x = innovation, short support; LTS = predictability for a single-user/single-disk setup) |
| Redis 8 | `redis:6379` | Keys `series:avg:{id}`, `season:avg:{id}` (TTL 3600) set directly via `\Redis` in `EpisodeRatedHandler` (not via a Symfony cache pool — the handler injects `\Redis`). The `cache.rate_limiter` pool is used by the RateLimiter; the `cache.search` pool caches Search results (HMAI-271) |
| RabbitMQ 4.x | `rabbitmq:5672` (AMQP), `:15672` UI (guest/guest) | Image pinned to `rabbitmq:4-management-alpine` (3.12 was EOL; the major pin `:4` keeps the supported 4.x line — Khepri metadata backend, quorum queues by default). Transport `async`, exchange `series_events` (topic) + **classic queues** (NOT mirrored — removed in 4.0; we do not use them, so the bump = low BC risk), retry 3× (1s→2s→4s, max 30s), DLQ `failed`. Metadata is ephemeral (no data volume) — a restart = a fresh broker, Messenger auto-declares the exchange/queues on connect |
| Worker Messenger | `messenger_worker` | `messenger:consume async --time-limit=3600 -vv` |
| Worker Scheduler | `scheduler_worker` | `messenger:consume scheduler_default --time-limit=3600 -vv` |
| Node (Encore build) | `node:24-alpine`, container `aihm-node-1` | Long-running `tail -f /dev/null`. `docker compose exec node npm ...` |
| Graylog 6.3 | `monitoring` profile, UI `:9000` (admin/admin), GELF UDP `:12201` | In `make up` (full stack); `make min-up` = lean without monitoring. The Monolog `series`/`auth` channels go through GELF — `gelf.transport` is wrapped in `IgnoreErrorTransportWrapper`, so no Graylog ≠ 500 (graceful degrade, logs dropped). The stack is coupled: `graylog/graylog:6.3` ↔ `mongo:7` (metadata; Graylog 6.3 supports 5.0.7–8.x, the 6→7 bump = a single major step) ↔ `opensearchproject/opensearch:2` (search; **Graylog 6.3 supports OpenSearch only 1.1–2.19.5, NOT 3.x** — `:2` stays for the foreseeable future). External OpenSearch via `GRAYLOG_ELASTICSEARCH_HOSTS` (Graylog Open, no Data Node). `GRAYLOG_*` env unchanged from 5.2; the `scripts/graylog-bootstrap.sh` API (`/api/system/inputs`, index_sets, streams) is stable in 6.x — no changes |

In tests: the `async` and `failed` transports → `in-memory://` (`when@test` in `messenger.yaml`).

Async messages routed to the `async` transport: `Series\Domain\Event\EpisodeRated`, `Series\Application\Command\ImportWatchedShowsFromTrakt` (import of watched shows from Trakt offloaded from the request, pinned by `ImportWatchedShowsRoutingTest`), `Series\Application\Command\ImportRatingsFromTrakt` (import of ratings from Trakt, chained after the watched import, pinned by `ImportRatingsRoutingTest`), `Music\Application\Command\RefreshDiscogsCollection` (fetch of the Discogs collection offloaded from the request, the `/api/music/collection` endpoint returns the cache + dispatches a refresh on a miss), `Music\Application\Command\PollLastFmRecentTracks` (scheduler poll every 30 min, the handler dispatches `LogListeningSession` per track on the sync command.bus), `Goals\Application\Command\RecalculateStreaks` (nightly scheduler recompute of the persisted streaks, HMAI-255; pinned by `RecalculateStreaksRoutingTest`). `Books\Domain\Event\BookCompleted` is deliberately sync (in-memory) — no handler, no I/O side-effects (ADR-006). Pinned by `BookCompletedRoutingTest`. `Search\Application\Command\ReindexSearchDocuments` is likewise deliberately sync (the 15-min FULLTEXT reindex runs inline in the scheduler worker, `command.bus`, not routed async) — pinned by `ReindexSearchDocumentsRoutingTest` (HMAI-265 epic review).

`NewRelicMonologHandler` (`src/Module/Series/Infrastructure/Logging/`) — graceful degrade when the `newrelic` extension is absent.

GELF UDP input + index sets + streams: `make monitoring-bootstrap` (the idempotent script `scripts/graylog-bootstrap.sh`). Creates the GELF UDP input, the `auth-events` (90 days, time-based) and `series-events` (30 days, time-based) index sets with the corresponding streams filtering by `channel`. Requires a running Graylog (`make monitoring-up` first).

## Symfony Scheduler

`src/Schedule.php` registers 7 recurring tasks (via `dragonmantank/cron-expression`):

| Cron | Message | Effect |
|---|---|---|
| `0 0 * * *` | `Articles\...\ResetDailyArticleCache` | Removes Redis `articles:today`, deletes `article_daily_picks` > 7 days old |
| `0 3 * * *` | `App\Application\Scheduled\BackupDatabase` | mysqldump + gzip → `/backups/homemanager-YYYY-MM-DD.sql.gz`, retention 30 daily + 12 monthly |
| `0 8 * * 1` | `App\Application\Scheduled\GenerateWeeklyActivityReport` | Logs `scheduled_task=weekly_report` to the default channel (read_articles, pages_read, completed_tasks, rated_episodes_total) |
| `0 */6 * * *` | `Music\...\RefreshDiscogsCollection` | Pre-warms the collection cache before the 6h TTL expires |
| `*/30 * * * *` | `Music\...\PollLastFmRecentTracks` | Polls Last.fm recent tracks → local listening history, idempotent via `dedup_hash` |
| `0 1 * * *` | `Goals\...\RecalculateStreaks` | Recomputes + persists the day-continuity `Streak` per activity type (async-routed, idempotent, preserves all-time longest); keeps streaks fresh across midnight breaks (HMAI-255) |
| `*/15 * * * *` | `Search\...\ReindexSearchDocuments` | Rebuilds the `search_documents` FULLTEXT index from the source modules via `SearchIndexer` (sync, idempotent); keeps global search fresh (HMAI-268) |

Worker: `bin/console debug:scheduler` shows the state; `docker compose up -d scheduler_worker` consumes the `scheduler_default` transport. Stateful via `cache.app` (filesystem, mounted on the host) — a worker restart fires at most 1 missed window (`processOnlyLastMissedRun(true)`).

### .env — key entries

```
DATABASE_URL=mysql://homemanager:homemanager@mysql:3306/homemanager?serverVersion=8.0&charset=utf8mb4
MESSENGER_TRANSPORT_DSN=amqp://guest:guest@rabbitmq:5672/%2f/messages
REDIS_URL=redis://redis:6379
GRAYLOG_HOST, GRAYLOG_PORT=12201
NEW_RELIC_LICENSE_KEY, NEW_RELIC_APP_NAME
```

## Makefile commands

| Action | Command |
|---|---|
| Start the environment (full stack + monitoring) | `make up` |
| Start the environment (lean, no monitoring) | `make min-up` |
| Full initialization | `make setup` |
| PHP shell | `make shell` |
| All tests | `make test` |
| Domain only | `make test-unit` |
| Integration only | `make test-integration` |
| Tests with coverage + threshold gate | `make test-coverage` |
| Cache clear | `make cc` |
| Migrations dev/test | `make migrate` / `make migrate-test` |
| Logs | `make logs` (all) / `make logs-{php,nginx,mysql,redis,rabbitmq,worker,scheduler,node}` (per-service) |
| Routing | `make routes` |
| Containers | `make services` |
| Worker status | `make messenger-status` |
| Preflight env health check | `make doctor` |
| Monitoring up/down/logs | `make monitoring-up` / `make monitoring-down` / `make monitoring-logs` |
| Graylog bootstrap (inputs+indexes+streams) | `make monitoring-bootstrap` |
| E2E (Playwright) install/run | `make test-e2e-install` / `make test-e2e` |
| Newman (Postman REST collection) | `make test-newman-install` / `make test-newman` |
| Load fixtures (dev) | `make fixtures` |
| Webpack Encore dev/watch/prod | `make assets` / `make assets-watch` / `make assets-prod` |
| JS unit tests (Vitest) | `make test-js` |
| Npm install (after a `package.json` change) | `make node-install` |
| npm audit (high+critical CVE gate) | `make node-audit` |
| Backup MySQL (manual) | `make backup-now` |
| Restore MySQL | `make restore BACKUP=backups/homemanager-YYYY-MM-DD.sql.gz` |

## Tests

- Unit: `tests/Unit/Module/{Name}/Domain/` — pattern `tests/Unit/Module/Series/Domain/SeriesAggregateTest.php`
- Integration: `tests/Integration/`
- Fixtures: `tests/Integration/DataFixtures/FixturesLoadTest.php` — checks that `make fixtures` produces a stable data structure
- E2E: `tests-e2e/` (Playwright, TypeScript). Files match `*.desktop.spec.ts` (1440×900) or `*.mobile.spec.ts` (Pixel 5 viewport) per the project config in `playwright.config.ts`
- Newman/Postman: `tests-e2e/postman/AIHomeManager.postman_collection.json`. Run via `make test-newman` (truncate + newman with `--ignore-redirects`); details in `tests-e2e/postman/README.md`
- JS unit (Vitest, HMAI-246): `app/assets/tests/*.test.js` — fast jsdom unit tests for the frontend **pure** functions (`util.js` `safeUrl`/`escHtml`/`TOAST_TIMEOUT_MS`; Series `sortSeries`/`filterSeries`/`ratingHighlight`/`ratingFlag`/`cardRatingFlag`/`cardRating`/`statusLabel`/`avg`). Run via `make test-js` (or `npm test` in `app/` → `vitest run`). CI: a "Run JS unit tests (Vitest)" step in the `tests` job, after `npm ci`/`npm audit`, before the Encore build. The tested pure helpers now live in `assets/series/{ratings,list}.js` — the view logic was extracted from the god-controller in HMAI-243 (`series_view.test.js` imports were repointed accordingly); `series_metadata.test.js` additionally covers `readMetadataInputs` (jsdom). Test files must NOT live under `assets/controllers/` (the `webpackContext` there registers every `.js` as a Stimulus controller)
- Framework: PHPUnit 13 + @playwright/test 1.61 + newman 6.x + Vitest 4 (JS unit, jsdom)
- **PHPUnit gates**: `phpunit.dist.xml` has `failOnDeprecation="true"` + `failOnPhpunitDeprecation="true"` + `failOnNotice="true"` + `failOnWarning="true"`. New PHP deprecations in `src/` AND PHPUnit's own deprecations (`->expects(self::any())`, `with()` without `expects()`, etc.) block CI. `<source>` has `ignoreIndirectDeprecations="true"` + `restrictNotices/Warnings="true"` — vendor noise (e.g. google/apiclient's `str_replace null` deprecation) is deliberately filtered. Notices are not on the gate — 41 are data noise from the tests, fix-effort vs value is weak. Locally: `vendor/bin/phpunit --display-phpunit-deprecations` shows the source PHPUnit deprecation; `--display-deprecations` shows the PHP deprecation
- `*ApiTest` tests use `App\Tests\Support\AuthenticatedApiTrait` — it adds the `X-API-Key: test-api-key` header (see `app/.env.test`) and exposes `jsonResponse(KernelBrowser): array<mixed>` (HMAI-247), which decodes the last response with a `string`+`array` assertion instead of raw `json_decode($response->getContent())` (the `string|false` of `getContent()` was ~75 baselined `argument.type` suppressions). Request bodies pass `content: (string) json_encode(...)`. **Mock-typing convention:** a test-double stored in a property must keep the double type in the declaration — `private Foo&Stub $foo;` (created via `createStub`) or `private Foo&MockObject $foo;` (`createMock`), NOT the bare `private Foo $foo;` — otherwise PHPStan erases the intersection to the interface and reports `method.notFound` on `->method()`/`->expects()` (local-var doubles keep the inferred intersection and need no annotation). `->method()` lives on PHPUnit's `Stub`, `->expects()` on `MockObject extends Stub`
- CI gate: the `tests` job runs `doctrine:schema:validate` after the migrations and before PHPUnit — a drift of the ORM XML mapping vs the MySQL schema blocks the merge (a separate error category, not baked into a test). Locally: `make schema-validate`
- **Coverage gate (HMAI-245)**: PHPUnit measures line coverage via **pcov** (driver added to `docker/php/Dockerfile`; `pcov.enabled=0` in `php.ini` so a bare `make test`/CI `vendor/bin/phpunit` pays **zero** overhead — enabled per-run with `-d pcov.enabled=1`). `phpunit.dist.xml` has a `<coverage includeUncoveredFiles ignoreDeprecatedCodeUnits>` element; reports stay opt-in via CLI flags (no `<report>` in the XML). Locally: `make test-coverage` → clover (`var/coverage/clover.xml`) + browsable HTML (`var/coverage/html`) + threshold check. The `tests` CI job sets `coverage: pcov`, runs PHPUnit with `--coverage-clover`/`--coverage-html`, **enforces a minimum line-coverage threshold** (fail-on-regression) and uploads the report as the `coverage-report` artifact. The gate is `app/bin/coverage-check.php <clover> <min-percent>` (parses the project `<metrics>` from clover, exits 1 below the floor) — shared by Makefile (`COVERAGE_MIN`, default **90**) and CI (the literal `90` in `ci.yml`, kept in sync). Baseline measured 2026-06-30 = **93.66%** (3976/4245 statements); the floor sits conservatively below it as an informational regression guard, to be tightened later
- E2E/Newman prerequisite: `API_KEY=e2e-test-key` in `app/.env.local`, Discogs/Last.fm placeholders (`DISCOGS_TOKEN_KEY`, `GOOGLE_TOKEN_KEY`, `DISCOGS_CONSUMER_KEY`, `DISCOGS_CONSUMER_SECRET`, `LASTFM_API_KEY`, `LASTFM_USERNAME`, `DISCOGS_USERNAME`) set to anything non-empty (DI will not boot with empty VOs). The Graylog GELF UDP input is worth configuring for full observability (`make up` now starts monitoring by default, then `make monitoring-bootstrap`; alternatively a manual POST to `/api/system/inputs` with `org.graylog2.inputs.gelf.udp.GELFUDPInput` on `0.0.0.0:12201`). A non-running Graylog does **not** blow up with 500 on `/api/series` anymore — `gelf.transport` is wrapped in `IgnoreErrorTransportWrapper`, so GELF transport errors are swallowed (the `series`/`auth` logs are silently dropped, the request goes on; **this applies to the `dev`/`prod` envs** — in `test` the channels go to `null` anyway). In CI the E2E/Newman jobs run with `APP_ENV=test`, where `monolog when@test` routes the `series`/`auth` channels to `null` handlers → Graylog is not needed. The `*_TOKEN_KEY` keys in CI are **valid base64 32B** (`TokenCipher` throws for any other length — an OAuth-init request would otherwise return 500 instead of 302/502). App server in CI: `symfony server:start --no-tls --port=8080` (serves routing + Encore static assets; a bare `php -S` does not combine the two)

## Security — API Key

- `^/api/*` protected by the `api` firewall in `app/config/packages/security.yaml` (stateless, custom authenticator) — the `pattern: ^/api` spans **both** the versioned `/api/v1/*` surface and the `/api/*` alias (ADR-008, HMAI-338), so no version-specific firewall rule is needed
- Authenticator: `App\Security\ApiKeyAuthenticator` — reads the `X-API-Key` header, compares via `hash_equals` with `%env(API_KEY)%`. `supports()` returns `false` for `/api/health` (public readiness probe) **and for the API-doc routes** `/api/doc`, `/api/doc.json`, `/api/doc/redoc` (`isDocumentationPath()` — the interactive OpenAPI docs are viewable without a key; see the API documentation section below).
- 401 JSON `{"error": "..."}` on a missing/invalid key
- Production key in `app/.env.local` (gitignored). `app/.env` has only a placeholder
- `/auth/google*`, `/auth/discogs*`, `/auth/trakt*`, frontend (`/`, `/series`, etc.) — the `main` firewall with `security: false` (public)
- Test env: `API_KEY=test-api-key` in `app/.env.test`
- **CSRF (ADR-005):** we deliberately **do not use** `#[IsCsrfTokenValid]` on `^/api/*`. The firewall is `stateless: true`, authorization via the `X-API-Key` header (not a cookie) — the browser does not set custom headers cross-origin, so CSRF has no path. OAuth init (`/auth/*`) uses the `state` parameter. Regression in `tests/Integration/Security/ApiKeyAuthCsrfTest.php`.

## API documentation — OpenAPI (HMAI-336…343, epic HMAI-311 — DONE)

- **NelmioApiDocBundle** (`nelmio/api-doc-bundle`, registered in `config/bundles.php`) generates a machine-readable OpenAPI **3.1** contract for `^/api/*` — chosen over API Platform to keep the hexagonal architecture + thin controllers untouched (annotations on existing controllers). Foundation for the mobile client's stable API contract.
- Config `config/packages/nelmio_api_doc.yaml`: base `documentation` (`openapi: 3.1.0`, `info` title/description/version, `servers: [{ url: /api/v1 }]` — the versioned base, ADR-008) + area `default` with `path_patterns: [ ^/api/v1 ]` — documents the **versioned** surface only (the `/api` alias mirrors it 1:1; the doc routes `/api/doc*` live outside `/api/v1` and are naturally excluded). `html_config.assets_mode: bundle` — Swagger UI / Redoc assets are served from the bundle's installed `public/bundles/nelmioapidoc/*` (via the `assets:install` Composer auto-script, which CI runs since `composer install` is **not** `--no-scripts`), NOT a CDN.
- Routes (`config/routes/nelmio_api_doc.yaml`, all `GET`): `/api/doc.json` (spec → `nelmio_api_doc.controller.swagger`), `/api/doc` (Swagger UI → `…swagger_ui`), `/api/doc/redoc` (Redoc → `…redocly`). All three are **public** (excluded from `ApiKeyAuthenticator::supports()`, mirroring `/api/health`) but still under the `^/api/*` per-IP rate limiter.
- **Shared contract components (HMAI-337):** `nelmio_api_doc.yaml` defines a `documentation.components` block reused by later per-module docs via `$ref` — its shapes mirror the code, NOT hand-invented: `securitySchemes.ApiKeyAuth` (`apiKey`/`header`/`X-API-Key` = `ApiKeyAuthenticator::HEADER`) set as the global default `security: [{ ApiKeyAuth: [] }]`; `schemas.Error` (`{ error: string }` = `ApiExceptionListener`'s envelope), `schemas.RateLimitError` (`allOf` Error + `retry_after` = the 429 body from `ApiRateLimitListener`), `schemas.Pagination` (reserved envelope — current list endpoints return the full set, none paginate yet); ready-to-`$ref` `responses` for the whole error surface (`UnauthorizedError` 401 / `NotFoundError` 404 / `ConflictError` 409 / `UnprocessableEntityError` 422 / `TooManyRequestsError` 429 / `InternalServerError` 500); `headers` `X-RateLimit-Limit`/`X-RateLimit-Remaining`/`Retry-After` (`ApiRateLimitListener`) + `X-Request-ID` (`RequestIdListener::HEADER_NAME`). The one **public** documented endpoint `/api/health` is defined inline in `documentation.paths` with `security: []` (opts out of the global key, mirroring `ApiKeyAuthenticator::supports()`) — it is the example endpoint wiring the shared components via `$ref` (X-Request-ID header, the 500 `InternalServerError` response). NelmioApiDocBundle deep-merges this raw `documentation` node onto the auto-discovered routes, so the `/api/health` operation-level `security: []` correctly overrides the root default.
- **API versioning (ADR-008, HMAI-338):** the API is served under the versioned base **`/api/v1`**, with the legacy **`/api`** prefix kept as a backward-compatibility **alias** (both resolve to the same controllers → byte-identical behavior). Versioning is by **path prefix** (not an `Accept`/header version) — simpler for clients, caches, and OpenAPI tooling. Mechanism: the six API module controllers live under `src/Controller/Api/` (`App\Controller\Api\*`) with **version-agnostic** route paths (`#[Route('/series')]`, not `/api/series`); `config/routes.yaml` imports that dir **twice** — `api_v1` (`prefix: /api/v1`, `name_prefix: api_v1_`) and `api_legacy` (`prefix: /api`, original route names retained) — so the prefix is applied centrally, never duplicated in attributes. The general `../src/` import **excludes** `../src/Controller/Api/`. **`/api/health` stays un-versioned** (infra probe, not part of the client contract, `HealthController` untouched in `src/Controller/`) — its OpenAPI path pins `servers: [{ url: / }]` to override the `/api/v1` base. **No change to `security.yaml` / `ApiRateLimitListener`:** the firewall `pattern: ^/api` and the listener's `str_starts_with($path, '/api/')` check already span `/api/v1/*` — documented here rather than making no-op edits that would narrow the pattern. **Compatibility policy:** the `/api` alias lives as long as any released client depends on it; a breaking contract change ships as `/api/v2` (a third route import) with `/api/v1` retained through a documented deprecation window, never mutated in place.
- **Per-module contracts (in progress):** the shared components are now `$ref`'d by real endpoint docs as each module is annotated. **Tasks is done (HMAI-340):** all nine `/api/v1/tasks*` operations (REST CRUD + `complete`/`cancel` + `time-report` + `export`) carry `#[OA\*]` attributes on `App\Controller\Api\TasksController` — query/path params, request bodies, success schemas via the Nelmio `#[Model(type: …DTO)]` ref (`TaskDTO`; `TimeReportDTO` whose `breakdown` resolves to `TaskTimeDTO` from the `@var list<…>` phpdoc), and error responses via `$ref` to the shared `#/components/responses/{NotFoundError,UnprocessableEntityError,UnauthorizedError}`. `export` documents both `text/csv` + `application/pdf` binary media types + the `format` enum; the `Tasks` operation tag is declared under `documentation.tags` in `nelmio_api_doc.yaml`. **The attributes are documentation-only — zero runtime change, the controller stays thin** (annotations sit above the existing `#[Route]`). The `@Model`-generated schemas mirror the real JSON byte-for-byte (`TaskDTO` = the `TaskDTONormalizer` field order; `TimeReportDTO` = the hand-built `timeReport()` array). **Series + `/api/health` are done (HMAI-339):** all sixteen `/api/v1/series*` operations (CRUD of series/seasons/episodes, disjoint series & season own ratings, episode `watched`, delete/rename/renumber, Trakt import 202/409) carry `#[OA\*]` attributes on `App\Controller\Api\SeriesController`, tagged `Series`, the success schema via `#[Model(type: SeriesDetailDTO::class)]` — its `seasons`→`SeasonDTO`→`episodes`→`EpisodeDTO` nesting resolves from `@param list<…>` phpdoc (the two Series DTOs' phpdoc was tightened `[]`→`list<>` so the array items `$ref` a clean `SeasonDTO`/`EpisodeDTO` instead of a `oneOf[array,map]`) — and errors via `$ref` to the shared responses (incl. `ConflictError` on renumber). `/api/health` is enriched **inline** in `documentation.paths` (it lives outside the `^/api/v1` area, so no attributes on `HealthController`) with the real 200 `healthy`/`degraded` + 503 `unhealthy` body schemas, kept public (`security: []`); its `Series`/`Health` tags are declared under `documentation.tags`. **Books + Articles are done (HMAI-341):** all `/api/v1/books*` (list/detail/create/update/delete/export/reading-sessions) and `/api/v1/articles*` (list/detail/create/update/delete/today/import/export/read) operations carry `#[OA\*]` attributes, tagged `Books`/`Articles`, with `#[Model(type: BookDTO|ArticleDTO|ReadingSessionDTO)]` success schemas — note `GET /books/{id}` is modelled as `allOf[BookDTO, {sessions}]` because `BookDetailDTONormalizer` **flattens** the book fields to the top level and appends `sessions` (it is NOT a `{book,sessions}` envelope); the article `import` documents a `multipart/form-data` file upload; `books` create carries the provider-unavailable 503; request bodies use the real snake_case keys (`cover_url`, `total_pages`, `estimated_read_time`) while responses stay camelCase (the normalizers). **Music + YouTubeProgress are done (HMAI-342):** all `/api/v1/music*` (top-albums/comparison/collection/history/sessions) and `/api/v1/youtube-progress*` (watchlist/sessions/sync/videos start+watched/push-to-youtube) operations carry `#[OA\*]` attributes, tagged `Music`/`YouTubeProgress`, with `#[Model(type: Album|VinylRecord|ListeningSessionDTO|VideoDTO|WatchSessionDTO)]` schemas (Album/VinylRecord are the Music **Domain read models**, HMAI-233; `WatchSessionDTO.videos`→`VideoDTO` resolves from the `list<>` phpdoc) — the external-integration codes are documented explicitly: `comparison` carries 429 (`TooManyRequestsError`) + 503, `top-albums`/`collection` 503, YT `sync` a 400 when the playlist id is unset, and the YT video/session actions the shared 404 (handlers throw `NotFoundHttpException`, unwrapped by `ApiExceptionListener`). **With HMAI-342 the OpenAPI contract now covers every `^/api/*` module** (Tasks, Series+health, Books, Articles, Music, YouTubeProgress) — no module is auto-discovered bare anymore.
- **The contract is a CI quality gate (HMAI-343) — closes epic HMAI-311.** A dedicated **5th CI job `openapi-contract`** dumps the static `openapi.json` (`bin/console nelmio:apidoc:dump --format=json` in `dev` with dummy DSNs — the dump never touches MySQL/Redis), publishes it as the `openapi-spec` build artifact (the input for the generated mobile client), and **lints it with Spectral** (`.spectral.yaml` at repo root extends `spectral:oas`; `--fail-severity=error` = a structurally invalid spec blocks the merge, while ~21 style findings stay non-blocking warnings). The one bundled rule **`duplicated-entry-in-enum` is disabled** because it crashes Spectral 6.x's nimma engine on this 3.1 document (`Cannot read properties of null (reading 'enum')` — every nullable property carries `default: null`); `typed-enum` and the rest run fine. **Spectral CLI is pinned `@stoplight/spectral-cli@6.16.1`** (run via `npx` on the CI runner's Node — NOT a project dep, so it stays out of the npm-audit gate). Response↔schema **conformance** is enforced in PHPUnit (`OpenApiContractTest`) using **`opis/json-schema`** (`require-dev`; native JSON Schema **2020-12** = OpenAPI 3.1's dialect — a `cebe/php-openapi`-based validator like `league/openapi-psr7-validator` only understands 3.0): the whole spec is registered under a base URI so intra-document `$ref`s resolve, each representative endpoint's body is validated against the schema documented **for the status code it actually returned** (robust when `/api/health` reports 503 in the test env), and a negative-control test proves a drifting payload is rejected. Local equivalents: `make openapi-dump` / `make openapi-lint`. The generated `openapi.json` is a **gitignored build artifact** (regenerated from the annotated controllers, never committed). **NOTE (manual, repo admin): add the `OpenAPI contract (dump + Spectral lint)` check to the branch-protection required-checks list** on develop+master so the 5th job becomes a hard merge gate (protection currently enumerates 4 checks; the job runs on every PR regardless).
- Regression: `tests/Integration/Api/ApiDocTest.php` (9 tests — spec is public + `openapi == "3.1.0"` + `info`/`servers`; Swagger UI & Redoc render as HTML; the `ApiKeyAuth` scheme + global `security`; the `Error` schema + all 6 error responses; the 429 retry contract + rate-limit/correlation headers; the `Pagination` schema; `/api/health` `security: []`) + `ApiVersioningTest` (4 tests — `/api/v1` ↔ `/api` identical payload, cross-prefix write visibility, `/api/v1` requires the key, `servers` advertises `/api/v1`) + `TasksApiDocTest` (7 tests — every Tasks operation+method is documented and `Tasks`-tagged; the `status` enum + `TaskDTO` array items; the create request body required fields + the 201 `id`; write endpoints `$ref` the shared 404/422; `time-report` → `TimeReportDTO`/`TaskTimeDTO` with required `from`/`to`; `export` offers `text/csv` + `application/pdf` binary; the `TaskDTO` schema mirrors the normalizer field order) + `SeriesApiDocTest` (9 tests — every Series operation+method documented & `Series`-tagged; list → array of `SeriesDetailDTO` + detail `$ref` + 404; create body `title` required + metadata props + `status` enum; the nullable rating body + 204/422; the episode `watched` boolean; Trakt import 202 `import_started` + 409 with `authUrl`; renumber 409 `ConflictError`; the `SeriesDetailDTO`→`SeasonDTO`→`EpisodeDTO` schema shape; `/api/health` public with 200 `healthy`/`degraded` + 503 `unhealthy`) + `BooksArticlesApiDocTest` (9 tests — every Books/Articles operation+method documented & tagged; books list → array of `BookDTO` + `status` enum; book detail `allOf[BookDTO, sessions→ReadingSessionDTO]` + 404; add-book `isbn` required + 503; books export both binary formats; reading-session `pages_read` required; articles list → `ArticleDTO` + today 200/204; import `multipart/form-data` file; create `title`+`url` required + shared 422/404) + `MusicYouTubeApiDocTest` (9 tests — every Music/YouTubeProgress operation+method documented & tagged; top-albums `period` enum + array of `Album` + 503; comparison `matchScore` + `Album`/`VinylRecord` buckets + 429; collection → `VinylRecord`; history `source` enum + date range → `ListeningSessionDTO`; log-session required body + 400; YT watchlist/sessions wrap `VideoDTO`/`WatchSessionDTO` with the nested videos; sync counts + 400; video/session actions 204 + shared 404) + `ApiKeyAuthTest` (doc routes bypass the key) + `OpenApiContractTest` (9 tests, HMAI-343 + HMAI-248 epic review — response↔schema conformance via `opis/json-schema`: `/api/health` (200 healthy/degraded or 503), series list + detail with populated seasons/episodes + own/season/episode ratings + `watched`, tasks list + detail, articles list, **goals list + streaks** (goal + seeded same-day activity → populated `GoalProgressDTO` achieved/percent/met and `StreakDTO` currentLength/non-null `lastActivityDate`) — each body validated against the schema documented for the status it returned; + a negative control proving a drifting response is **rejected**). The Goals cases close the epic-review gap where Goals was the only documented `^/api/*` module carrying `#[OA\*]` docs + Spectral lint but not yet under the runtime conformance gate.

## HTTP security headers

Dual-layer defense-in-depth: nginx (`docker/nginx/default.conf`) + the Symfony `SecurityHeadersListener` (`kernel.response`, priority -128). Both set the same 4 headers on every response:

| Header | Value |
|---|---|
| `X-Content-Type-Options` | `nosniff` |
| `X-Frame-Options` | `DENY` |
| `Referrer-Policy` | `strict-origin-when-cross-origin` |
| `Permissions-Policy` | `geolocation=(), microphone=(), camera=(), payment=()` |

HSTS (`Strict-Transport-Security`) is commented out in nginx — uncomment AFTER configuring HTTPS. `server_tokens off` hides the nginx version.

Regression: `tests/Integration/Security/SecurityHeadersTest.php` (4 tests: frontend, API, error 404, all headers).

## Request correlation

- `App\EventListener\RequestIdListener` — `kernel.request` priority 256 (before `ApiRateLimitListener` @100, so that a 429 carries the correlator), `kernel.response` priority 0. Reads the `X-Request-ID` header from the request or generates a UUID v4. It stores the value in the `_request_id` request attribute and echoes it back in the response header.
- Inbound validation: `^[A-Za-z0-9._-]{1,128}$`. Values outside this set are rejected (a server-generated UUID replaces them) — protection against injecting control characters into the logs.
- `App\Logging\RequestIdProcessor` — invokable, `#[AsMonologProcessor]`. Reads `_request_id` from `RequestStack->getMainRequest()` and adds `extra.request_id` to every `LogRecord` emitted during the request. CLI/worker context (no main request) — passthrough.
- Async (Messenger): propagating the ID to the worker is deliberately out of scope — it requires a separate Stamp + middleware.
- Regression: `tests/Integration/EventListener/RequestIdListenerTest.php` (4 tests: no header, valid echo, invalid replaced, log extra carry).

## API exception listener

- `App\EventListener\ApiExceptionListener` — `kernel.exception` (priority 64, before the framework `ErrorListener` at -64). Converts uncaught throwables on `^/api/*` into a `JsonResponse`.
- `HttpExceptionInterface` (4xx) preserves the status and message; everything else (`RuntimeException`, a `DomainException` outside the controller's catch, etc.) → 500 with a generic `Internal server error.` (the original message only in the log, not in the response).
- `HandlerFailedException` (the Messenger wrap) is unwrapped — the listener uses the previous exception for type checks, so HTTP exceptions from handlers are caught the same as those thrown directly.
- Non-API paths (e.g. `/series`, `/typo`) pass through unchanged — the Twig frontend keeps its rendered error pages.
- The full exception context (path, method, status, exception) is logged at the `error` level via the default channel.

## Health endpoint

- `GET /api/health` — a public readiness probe (no `X-API-Key`)
- Probes: MySQL (`SELECT 1`), Redis (`PING`), RabbitMQ (TCP to the host from `MESSENGER_TRANSPORT_DSN`, timeout 1s), Disk (`disk_free_space('/')`)
- 200 `{"status":"healthy", "components":{"mysql":"up", "redis":"up", "rabbitmq":"up", "disk":"up"}, "timestamp":"..."}` when everything is up
- 503 `"status":"unhealthy"` + a `"down"` component when a probe fails (or disk >95% used) — orchestrators do not route traffic to a degraded instance
- The **disk probe** has 3 states, the rest are still binary up/down:
    - `< 80% used` → `up`
    - `80–95% used` → `degraded` (HTTP 200, `status: "degraded"` in the body — monitoring pages before escalation, traffic still routed)
    - `> 95% used` → `down` (HTTP 503 — MySQL flush/binlog dies with no headroom, make room BEFORE the server crashes)
    - Thresholds hardcoded as consts in `HealthChecker` (`DISK_DEGRADED_RATIO=0.80`, `DISK_DOWN_RATIO=0.95`). YAGNI on an ENV override — a single instance, one disk, one problem.
    - `disk_free_space('/')` measures Docker's overlayfs → reflects the host's space (single-volume setup). Multi-volume (MySQL on a separate data volume) would need a separate probe — out of scope.
- Docker healthcheck on `nginx`: `wget --spider http://localhost/api/health` (interval 30s, retries 3, start_period 30s) — an end-to-end stack probe
- `HealthChecker` (`src/Health/HealthChecker.php`) — `readonly` (NOT `final` so that PHPUnit `createStub` works in the controller test)

## Static Analysis

- **PHPStan** level 8 + `phpstan-symfony` + `phpstan-doctrine` + `phpstan-phpunit`. Config: `app/phpstan.neon.dist`. Baseline `app/phpstan-baseline.neon` — new errors require a fix or extending the baseline via `make phpstan-baseline`. The tech-debt epic HMAI-232 shrank the baseline (HMAI-244 cleared every `src/` `missingType.iterableValue`; HMAI-247 the `*ApiTest` `json_decode` suppressions; the HMAI-232 review removed two dead tautologies — `NewRelicMonologHandlerTest`, `tests/bootstrap.php`). **HMAI-274** finished that reduction — the ~26 residual entries were fixed at the source, not re-baselined (production: DiscogsAuthController OAuth1 `array|string` narrowed via `is_string`, `GetMusicComparisonHandler` redundant `??` + `array_values` list casts, `ApiUser` `non-empty-string`, `ApiUserProvider` `@implements UserProviderInterface<ApiUser>`, `NationalLibraryApiClient` SimpleXMLElement guard, `ArticleImporter` `array_map` callable; plus test-helper phpdoc + nullable-`find()`/`findEpisode()` `assertNotNull`). `phpstan-baseline.neon` now holds exactly ONE deliberately-kept entry: `ApiKeyAuthTest`'s `assertSame('X-API-Key', ApiKeyAuthenticator::HEADER)` is a PHPStan tautology (the constant resolves to its literal) but a legitimate wire-contract regression guard
- **PHP CS Fixer**: `@Symfony` + `@PHP84Migration` + `global_namespace_import` (classes imported). Config: `app/.php-cs-fixer.dist.php`
- **Rector**: `withPhpSets()` + `deadCode`. Config: `app/rector.php`
- **Deptrac**: formalizes the hexagonal boundaries — every module has separate `*Domain` / `*Application` / `*Infrastructure` layers, plus a cross-cutting `Shared` kernel layer (`src/Shared/.*`, HMAI-235). Domain → [`Shared`] (otherwise zero dependencies beyond PHP core), Application → its own Domain + `Shared` + Vendor, Infrastructure → its own Domain + its own Application + `Shared` + Vendor, `Shared` → [] (pure VOs + contracts), `Glue` (Controllers/EventListeners/Kernel/Security outside `src/Module/` and `src/Shared/`) → everything. Cross-module coupling forbidden (the shared kernel is the sole sanctioned cross-context reuse). Config: `app/deptrac.yaml` now runs with **zero `skip_violations`** — the architecture is violation-free. The four Domain-port→Application-DTO entries were dropped once the ports moved to `Domain\ReadModel\` (HMAI-233); the three `App\Security\TokenCipher` entries were dropped once Music/Tasks/Series Infrastructure moved to the Shared `App\Shared\Security\TokenCipherInterface` port (HMAI-234); the last YouTube adapter → Tasks `GoogleTokenRepositoryInterface` Google-token entry was dropped once the YouTube adapter moved to the Shared `App\Shared\Security\GoogleTokenProviderInterface` read port (HMAI-237). Regeneration: `make deptrac-baseline` → move `skip_violations` from `deptrac-baseline.yaml` into `deptrac.yaml` and delete the separate file (single source of truth)
- **Composer audit**: `composer audit` (built in since 2.4) queries FriendsOfPHP/security-advisories. CI gate in `static-analysis` after Deptrac — blocks the merge when an advisory appears for an installed package version. Locally: `make audit`. A failure = bump the dep, do not suppress (an advisory failing CI is a legit signal)
- **Dependabot**: `.github/dependabot.yml` — 4 ecosystems: composer (`/app`, weekly Mon, groups `symfony/*`/`doctrine/*`/dev), npm (`/app` + `/`, weekly), github-actions (`/`, monthly). PRs from `dependabot[bot]` go through the same CI gate as user commits — review + merge when green. Dependabot covers freshness, `composer audit`/`npm audit` covers severity-gated regression — complementary, they do not replace each other
- CI: `.github/workflows/ci.yml` — 5 jobs on every push/PR: `static-analysis` (Rector dry-run + CS Fixer + PHPStan level 8 + Deptrac + Composer audit), `openapi-contract` (dump `openapi.json` → Spectral lint → upload `openapi-spec` artifact, HMAI-343), `tests` (PHPUnit + coverage threshold gate, HMAI-245), `e2e-playwright` and `e2e-newman` (both `needs: tests`)
- **CI job timeouts**: every job has an explicit `timeout-minutes` — `static-analysis: 10`, `tests: 15`, `e2e-playwright: 20`, `e2e-newman: 10`. The cap = ~2–3× the observed peak. The GitHub Actions default is 360 min — a runaway/deadlock with no bound eats the whole free-minutes budget on a single hang. After 30 days monitor the real times: if a job approaches its bound (>70%), raise it — do not lower it, because flaky CI is worse than a timeout

| Command | Action |
|---|---|
| `make analyse` | CS Fixer (dry-run) + PHPStan + Deptrac |
| `make phpstan` | PHPStan analyse |
| `make phpstan-baseline` | Regenerate the baseline (after fixing errors) |
| `make cs-check` / `cs-fix` | CS Fixer dry-run / apply |
| `make rector-dry` / `rector` | Rector dry-run / apply |
| `make deptrac` | Deptrac analyse (architecture boundaries) |
| `make deptrac-baseline` | Regenerate the deptrac baseline |
| `make schema-validate` | Doctrine schema validate (ORM XML mapping ↔ MySQL schema) |
| `make audit` | Composer audit (security advisories) |
| `make openapi-dump` | Dump the generated OpenAPI 3.1 contract → `openapi.json` (gitignored) |
| `make openapi-lint` | Dump + Spectral lint the contract (error severity blocks) |

## Rate limiting — own API + external APIs

- `App\EventListener\ApiRateLimitListener` — `kernel.request` (priority 100, before the router/firewall). Per-IP throttle for `^/api/*` (limiter `api_per_ip`, sliding_window 60/min). Bypass: `/api/health` and `/auth/*`. A 429 returns `Retry-After`, `X-RateLimit-Remaining`, `X-RateLimit-Limit`. Logs `rate_limit_triggered=true` (warning).
- `App\Http\RateLimitedHttpClient` — an `HttpClientInterface` decorator that proactively blocks the request before calling the external API (`reservation->wait()`). Five instances in `services.yaml`: `app.discogs_http_client`, `app.lastfm_http_client`, `app.national_library_http_client`, `app.youtube_http_client`, `app.trakt_http_client` — injected into the respective Music/Books/YouTubeProgress/Series clients.
- Limiters (`app/config/packages/rate_limiter.yaml`): `api_per_ip` (sliding_window, 60/min), `discogs_api` (token_bucket, 60/min), `lastfm_api` (token_bucket, 5/s), `national_library_api` (token_bucket, 60/min), `youtube_api` (token_bucket, 60/min — a soft HTTP fallback under the unit-based YT quota of 10000/day), `trakt_api` (token_bucket, 1000/5min — the Trakt import client)
- `TraktApiClient` (`Series/Infrastructure/External`): `fetchWatchedShows()` → `GET https://api.trakt.tv/sync/watched/shows?extended=full` with the headers `trakt-api-version: 2` + `trakt-api-key: {TRAKT_CLIENT_ID}` + `Authorization: Bearer {access_token}` (the token read from `TraktTokenRepositoryInterface::get()`, NOT from ENV). Returns `list<array{traktId:int, title:string, year:?int, seasons:list<...episodes...>}>` (no cache — the import reads fresh state; Trakt does not provide episode titles here, only numbers + `last_watched_at`). `RuntimeException` when there is no token ("not connected") / an empty client ID / a transport error. The result is consumed by the Trakt library import into the Series module. `fetchRatings()` → three GETs `/sync/ratings/{shows,seasons,episodes}` (the same headers), returns `array{shows,seasons,episodes}` with ratings 1–10 (the `RatingsProviderInterface` port); a shared private `get()` does the token/client guard + error handling for both methods
- Trakt import → Series: command `ImportWatchedShowsFromTrakt` (`Series/Application/Command`, **no payload** — single-user) + handler `#[AsMessageHandler(bus:'command.bus')]`, routing `async` (RabbitMQ; `in-memory` in tests, pinned by `ImportWatchedShowsRoutingTest`). Maps watched shows from Trakt onto the `Series` aggregate (+ seasons + episodes with the `watched` flag and a real `watchedAt` from `last_watched_at`), **idempotently**: dedup the show by `trakt_id`, the season by `number` within the show, the episode by `number` within the season — a re-import does not create duplicates nor overwrite already-watched episodes (`save()` only when something changed). The criterion for importing a show = ≥1 watched episode; empty shows/seasons are not materialized. Trakt does not provide episode titles here → placeholder `Episode {n}` (the user can change it). No Trakt token → a `RuntimeException` from the client propagates (worker retry/DLQ; UX handling in a separate layer). Regression: `ImportWatchedShowsFromTraktHandlerTest` (fresh import / idempotency / match-by-`trakt_id` + flip watched / skip an empty show / propagation of a missing token)
- Trakt ratings import: command `ImportRatingsFromTrakt` (`Series/Application/Command`, **no payload**) + handler, routing `async` (pinned by `ImportRatingsRoutingTest`), **chained at the end** of `ImportWatchedShowsFromTraktHandler` (ratings go after the watched ones, once the shows/seasons/episodes already exist — a single "Import from Trakt" button does both). Maps Trakt ratings 1–10 onto the aggregate's **own** ratings (`Series::rate()`/`rateSeason()`/`rateEpisode()`), **skip-if-missing** (a rating for a non-imported show/season/episode is skipped — no materialization of unwatched entities) + **idempotently** (`save()` only when the rating differs from the stored one; grouping by `trakt_id` → 1 load/save per show). Episode ratings deliberately do **NOT** dispatch `EpisodeRated` (bulk = thousands of Redis-recompute events; averages computed in the read layer `SeriesRowHydrator`, HMAI-242). Regression: `ImportRatingsFromTraktHandlerTest` (apply show/season/episode / skip-missing / idempotency), `TraktApiClientTest::testParsesRatingsIntoStructuredShape`, the chain in `ImportWatchedShowsFromTraktHandlerTest::testChainsRatingsImportAfterWatchedShows`
- Storage: the `cache.rate_limiter` pool (Redis) in prod/dev. In tests `Symfony\Component\RateLimiter\Storage\InMemoryStorage` — not tagged `kernel.reset`, so the state survives request → request when `KernelBrowser::disableReboot()`. External limiters in tests use the `no_limit` policy
- Distributed lock: `LOCK_DSN=redis://redis:6379` (`.env`) — coordination web ↔ worker
- `DiscogsApiClient::fetchAllPages` — throttling is now done by `RateLimitedHttpClient`
- Exceptions/boundaries: `/auth/*` is outside `^/api/*` so the listener does not touch it; `/api/health` is explicitly excluded; the Google Calendar SDK uses its own HTTP client (not Symfony) and is NOT covered by the decorator — the 1M/day limit leaves a wide margin

## Encryption — OAuth tokens

- `App\Security\TokenCipher` (libsodium `secretbox`, format: base64(nonce ‖ ciphertext)) — a shared tool for all OAuth providers. It implements the shared-kernel port `App\Shared\Security\TokenCipherInterface` (`src/Shared/Security/`, HMAI-234): the module token repos type-hint the **interface**, not the glue class, so Infrastructure → Shared (no cross-module reach into Glue); DI binds the concrete `TokenCipher` instances per key (below)
- Three instances in `services.yaml`: `app.discogs_token_cipher` (key `DISCOGS_TOKEN_KEY`), `app.google_token_cipher` (`GOOGLE_TOKEN_KEY`) and `app.trakt_token_cipher` (`TRAKT_TOKEN_KEY`) — separate keys split the blast radius
- 32B base64 keys in `.env.local`. Generate: `php -r "echo base64_encode(sodium_crypto_secretbox_keygen());"`
- Discogs OAuth1: `DiscogsTokenRepository` (Music) — field-by-field encryption (`oauth_token`, `oauth_token_secret`)
- Google OAuth2: `GoogleOAuthTokenRepository` (Tasks) — encrypts the whole `token_json` (access+refresh+expires). Scope claims are cumulative on the refresh token: `calendar.events` (Tasks) + `youtube` (YouTubeProgress, full read/write — required by `createPlaylist`). One token, two modules — `GoogleClientFactory::create()` requests both scopes, `setPrompt('consent')` forces a re-consent. **After widening the scope with `youtube` the user MUST go through `/auth/google` once** to obtain a token with the widened scope — without it a YT API call returns 403. Regression: `tests/Integration/Module/YouTubeProgress/GoogleClientYouTubeScopeTest.php`
- Trakt OAuth2: `TraktOAuthTokenRepository` (Series/Infrastructure) — encrypts the whole `token_json`, the Google pattern. Flow `/auth/trakt` (302→`trakt.tv/oauth/authorize` with `state` in the session) + `/auth/trakt/callback` (code→token via `HttpClientInterface`, encrypted save, redirect `/series`). `TraktTokenProvider::getValidAccessToken()` does refresh-on-expiry (grant `refresh_token`) — layer 2 (TraktApiClient) injects the provider instead of the repo. The `trakt_oauth_tokens` table (DBAL, non-ORM) is excluded from `doctrine.dbal.schema_filter` like `google_oauth_tokens`. ENV: `TRAKT_CLIENT_ID`/`TRAKT_CLIENT_SECRET`/`TRAKT_REDIRECT_URI`. Regression: `tests/Integration/Auth/TraktAuthControllerTest.php`, `tests/Unit/Module/Series/Infrastructure/TraktTokenProviderTest.php`

## MCP servers (`.mcp.json`)

- `sequential-thinking` (npx)
- `github` (npx — requires `GITHUB_PERSONAL_ACCESS_TOKEN`)
- `context7` (npx — Symfony/Doctrine/PHP docs)
- `filesystem` (npx — root: AIHM)
- `mysql` (npx — `@benborla29/mcp-server-mysql`, `127.0.0.1:3306`, read-only: INSERT/UPDATE/DELETE disabled)
- `playwright` (npx — `@playwright/mcp@latest`, browser automation/E2E)
- `redis` (npx — `@modelcontextprotocol/server-redis`, `redis://127.0.0.1:6379`)
- `docker` (uvx — `mcp-server-docker`, requires `uv` on the host: `pipx install uv` or `winget install astral-sh.uv`)
- Atlassian Rovo: configured through claude.ai (NOT `.mcp.json`)
- Requirement: Node.js v18+ (v24.x LTS installed); the Docker MCP additionally requires `uv`

## Skills useful for the project

- `/start-task <KEY>` — Jira → branch → implement → PR → Confluence → transition (workflow with preferences: skip STOP checkpoints, no Co-Authored-By)
- `/review`, `/security-review` — review pending changes / security
- `superpowers-symfony:symfony-tdd-pest` or `:symfony-tdd-phpunit` — TDD RED/GREEN/REFACTOR
- `superpowers-symfony:symfony-check` — PHP-CS-Fixer + PHPStan + tests
- `superpowers-symfony:doctrine-architect` (subagent) — schema design
- `superpowers-symfony:symfony-reviewer` (subagent) — review after a code change
- `superpowers-symfony:functional-tests` — WebTestCase + TDD for controllers

## Rules for working with Claude Code

1. First read CLAUDE.md and describe the plan before implementing
2. One Jira task = one session
3. After every code change: `make test`. Do not report readiness if the tests do not pass
4. Before `git commit`: show the diff + a proposed commit message, do NOT commit without approval
5. Do NOT add `Co-Authored-By: Claude` in commits (preference)
6. Branches: `<KEY>-short-description` from `develop`
7. After a larger step (a closed ticket / epic review / release) **propose** `/compact` to the user — do not run it automatically

## Links

- Confluence hub: https://honemanager.atlassian.net/wiki/spaces/H/pages/46661633
- Repo: `zlotylesk/AIHomeManager` (GitHub)
